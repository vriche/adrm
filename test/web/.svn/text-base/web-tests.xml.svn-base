<!-- ======================================================================= -->
<!-- $Id: web-tests.xml,v 1.1 2007/04/05 01:48:16 cvsuser Exp $               -->
<!-- ======================================================================= -->
<!DOCTYPE project [
    <!ENTITY config SYSTEM "file:./config.xml">
    <!ENTITY login SYSTEM "file:./login.xml">
]>
<project basedir="." default="run-all-tests"> 
    <!-- Use i18n bundle, 2nd one listed is the default -->
    <property file="${build.dir}/test/properties/ApplicationResources_${user.language}.properties"/>
    <property file="web/WEB-INF/classes/ApplicationResources.properties"/>

    <!-- runs all targets -->
    <target name="run-all-tests" 
        depends="Login,Logout,PasswordHint,Signup,UserTests,FlushCache,FileUpload,ResourceLimitTests,ForetArrearageTests,PublishArrangeDetailTests,PublishArrangeTests,SysSequenceTests,MatterTypeTests,SysParamTests,SysMenuTests,OaTeleExpensesTests,ContractTargetMonthTests,ResourceSortTests,SysResourceTests,OaWorkFlowMoveTypeTests,OaWorkFlowCheckStateTests,SysUserTypeTests,SysPromptModeTests,SysEventTypeTests,SysEventStateTests,SysEventTests,SysEnentTypeTests,SysEnentStateTests,OaWorkFlowTypeTests,OaWorkFlowCheckResultTests,OaWorkFlowCheckTests,OaWorkFlowTests,OaScratchpadTests,OaProductUsedTests,OaProductTypeTests,OaProductInfoTests,OaInfoTypeTests,OaInfoTests,OaDocumentFileTests,OaDocumentCatalogPermitTypeTests,OaDocumentCatalogTests,OaDocumentTests,OaCalendarEventTests,OaBusinessCardTypeTests,OaBusinessCardTests,OaAssetsTypeTests,OaAssetsStateTests,OaAssetsTests,OaAreaPcTests,OaAreaCityTests,OaApplyInfoTests,ContractTargetTests,BranchTests,BranchTests,BranchTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,OrgTests,ContractTests,OrderLogTests,OrderDetailTests,OrderDayInfoTests,OrderCategoryTests,OrderTests,ContractPaymentTests,ContractTests,OrderLogTests,OrderDetailTests,OrderDayInfoTests,OrderCategoryTests,OrderTests,ContractPaymentTests,ContractTests,OrderLogTests,OrderDetailTests,OrderDayInfoTests,OrderCategoryTests,OrderTests,ContractPaymentTests,ContractTests,ContractPaymentTests,ContractTests,OrderLogTests,OrderDetailTests,OrderDayInfoTests,OrderCategoryTests,OrderTests,ContractPaymentTests,ContractTests,OrderLogTests,OrderDetailTests,OrderDayInfoTests,OrderCategoryTests,OrderTests,ContractPaymentTests,ContractTests"
        description="Call and executes all test cases (targets)"/>
    
    <!-- runs user-related tests -->
    <target name="UserTests" 
        depends="EditUser,SearchUser,SaveUser,AddUser,ActiveUsers"
        description="Call and executes all user test cases (targets)">
        <echo>Successfully ran all User JSP tests!</echo>
    </target>
    
    <!-- Login to the application -->
    <target name="Login" 
        description="Runs login test and verifies Main Menu's Title">
        <webtest name="login">
            &config;
            <steps>
                &login;
            </steps>
        </webtest>
    </target>

    <!-- Logout of the application -->
    <target name="Logout"
        description="Runs logout test and verifies Login's Title">
        <webtest name="login">
            &config;
            <steps>
                &login;
                <invoke description="get Logout Page" url="/logout.jsp"/>
                <verifytitle description="we should see the login title"
                    text=".*${login.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Retrieve Password Hint -->
    <target name="PasswordHint" 
        description="Gets the user's password hint">
        <webtest name="passwordHint">
            &config;
            <steps>
                <invoke description="get Login Page" url="/"/>
                <verifytitle description="we should see the login title" 
                    text=".*${login.title}.*" regex="true"/>
                <invoke description="click password hint link"
                    url="/passwordHint.html?username=mraible"/>
                <verifytitle description="we should see the login title again"
                    text=".*${login.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Verify the edit user screen displays without errors -->
    <target name="EditUser" 
        description="Tests selecting the 'Edit Profile' forward">
        <webtest name="editUser">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Profile link" url="/editProfile.html"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Verify the user list screen displays without errors -->
    <target name="SearchUser" 
        description="Tests search for and displaying all users">
        <webtest name="searchUser">
            &config;
            <steps>
                &login;
                <invoke description="click View Users link" url="/users.html"/>
                <verifytitle description="we should see the user list title" 
                    text=".*${userList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Edit a user and then save -->
    <target name="SaveUser" 
        description="Tests editing and saving a user">
        <webtest name="saveUser">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Profile link" url="/editProfile.html"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
                <setinputfield description="set address" name="addressForm.address" value="2255 Lane Avenue"/>
                <setinputfield description="set zipcode" name="addressForm.postalCode" value="59826"/>
                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="display Main Menu if save OK"
                     text=".*${mainMenu.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Add a new user after logging in -->
    <target name="AddUser"
        description="Adds a new user profile">
        <webtest name="addUser">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" 
                    url="/editUser.html?method=Add&amp;from=list"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
                    
                <setinputfield description="set username" name="username" value="newuser"/>
                <setinputfield description="set password" name="password" value="test"/>
                <setinputfield description="set confirmPassword" name="confirmPassword" value="test"/>
                <setinputfield description="set firstName" name="firstName" value="Test"/>
                <setinputfield description="set lastName" name="lastName" value="Name"/>
                <setinputfield description="set city" name="addressForm.city" value="Denver"/>
                <setinputfield description="select province" name="addressForm.province" value="CO"/>
                <setselectfield description="select country" name="addressForm.country" value="US"/>
                <setinputfield description="set postalCode" name="addressForm.postalCode" value="80210"/>
                <setinputfield description="set email" name="email" value="newuser@raibledesigns.com"/>
                <setinputfield description="set website" name="website" value="http://raibledesigns.com"/>
                <setinputfield description="set passwordHint" name="passwordHint" value="test"/>
                <setcheckbox description="select roles" name="userRoles" checked="user"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                
                <verifytitle description="Redisplay add user page" text=".*${userProfile.title}.*" regex="true"/>
                <verifytext description="verify success message" regex="true"
                    text='&lt;div class="message.*&gt;.*&lt;strong&gt;Test Name&lt;/strong&gt;.*&lt;/div&gt;'/>
                    
                <!-- Delete user -->
                <invoke description="Click edit user link" url="/editUser.html?username=newuser&amp;from=list"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytext description="verify success message" regex="true"
                    text='&lt;div class="message.*&gt;.*&lt;strong&gt;Test Name&lt;/strong&gt;.*&lt;/div&gt;'/>
                <verifytitle description="display user list" text=".*${userList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Sign up a new user (no login) -->
    <target name="Signup" description="Signup a new user">
        <webtest name="signup">
            &config;
            <steps>
                <!-- Logout in case user is already signed in -->
                <invoke description="get Logout Page" url="/logout.jsp"/>

                <invoke description="click signup link" url="/signup.html"/>
                <verifytitle description="view signup title" text=".*${signup.title}.*" regex="true"/>
                
                <setinputfield description="set username" name="username" value="signup-user"/>
                <setinputfield description="set password" name="password" value="test"/>
                <setinputfield description="set confirmPassword" name="confirmPassword" value="test"/>
                <setinputfield description="set firstName" name="firstName" value="Test"/>
                <setinputfield description="set lastName" name="lastName" value="Name"/>
                <setinputfield description="set city" name="addressForm.city" value="Denver"/>
                <setinputfield description="set state" name="addressForm.province" value="CO"/>
                <setselectfield description="select country" name="addressForm.country" value="US"/>
                <setinputfield description="set postalCode" name="addressForm.postalCode" value="80210"/>
                <setinputfield description="set email" name="email" value="signup-user@raibledesigns.com"/>
                <setinputfield description="set website" name="website" value="http://raibledesigns.com"/>
                <setinputfield description="set passwordHint" name="passwordHint" value="test"/>
                <clickbutton label="${button.register}" description="Click button 'Signup'"/>

                <verifytitle description="view main menu" text=".*${mainMenu.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${user.registered}"/>
            </steps>
        </webtest>
    </target>

    <!-- View Active Users -->
    <target name="ActiveUsers" 
        description="View Active Users List">
        <webtest name="activeUsers">
            &config;
            <steps>
                &login;
                <invoke description="get activeUsers URL" url="/activeUsers.html"/>
                <verifytitle description="we should see the activeUsers title" 
                    text=".*${activeUsers.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Flush OSCache -->
    <target name="FlushCache" 
        description="opens JSP to Flush Page Cache">
        <webtest name="flushCache">
            &config;
            <steps>
                &login;
                <invoke description="get flushCache URL" url="/flush.html"/>
                <verifytitle description="we should see the flush cache title" 
                    text=".*${flushCache.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- File Upload -->
    <target name="FileUpload" description="Tests uploading a file">
        <webtest name="fileUpload">
            &config;
            <steps>
                &login;
                <invoke description="click Upload a File link" url="/selectFile.html"/>
                <verifytitle description="we should see file upload form" text=".*${upload.title}.*" regex="true"/>
                <setinputfield description="set name" name="name" value="Canoo Test File"/>
                <setFileField description="set file" name="file" fileName="${basedir}/build.properties"/>
                <clickbutton label="${button.upload}" description="Click Upload"/>
                <verifytitle description="show file data if upload OK" text=".*${display.title}.*" regex="true"/>
                <verifytext description="verify original filename displayed on page" text="build.properties" />
            </steps>
        </webtest>
    </target>































































<!--ContractPayment-START-->
    <!-- runs contractPayment-related tests -->
    <target name="ContractPaymentTests"
        depends="SearchContractPayments,EditContractPayment,SaveContractPayment,AddContractPayment,DeleteContractPayment"
        description="Call and executes all contractPayment test cases (targets)">
        <echo>Successfully ran all ContractPayment JSP tests!</echo>
    </target>

    <!-- Verify the edit contractPayment screen displays without errors -->
    <target name="EditContractPayment"
        description="Tests editing an existing ContractPayment's information">
        <webtest name="editContractPayment">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractPayment link" url="/editContractPayment.html?id=1"/>
                <verifytitle description="we should see the contractPaymentDetail title"
                    text=".*${contractPaymentDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a contractPayment and then save -->
    <target name="SaveContractPayment"
        description="Tests editing and saving a contractPayment">
        <webtest name="saveContractPayment">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractPayment link" url="/editContractPayment.html?id=1"/>
                <verifytitle description="we should see the contractPaymentDetail title"
                    text=".*${contractPaymentDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${contractPaymentDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new ContractPayment -->
    <target name="AddContractPayment"
        description="Adds a new ContractPayment">
        <webtest name="addContractPayment">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editContractPayment.html"/>
                <verifytitle description="we should see the contractPaymentDetail title"
                    text=".*${contractPaymentDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="ContractPayment List appears if save successful"
                    text=".*${contractPaymentList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contractPayment.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing contractPayment -->
    <target name="DeleteContractPayment"
        description="Deletes existing ContractPayment">
        <webtest name="deleteContractPayment">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractPayment link" url="/editContractPayment.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display ContractPayment List" text=".*${contractPaymentList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contractPayment.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchContractPayments"
        description="Tests search for and displaying all contractPayments">
        <webtest name="searchContractPayments">
            &config;
            <steps>
                &login;
                <invoke description="click View ContractPayments link" url="/contractPayments.html"/>
                <verifytitle description="we should see the contractPaymentList title"
                    text=".*${contractPaymentList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--ContractPayment-END-->

<!--Order-START-->
    <!-- runs order-related tests -->
    <target name="OrderTests"
        depends="SearchOrders,EditOrder,SaveOrder,AddOrder,DeleteOrder"
        description="Call and executes all order test cases (targets)">
        <echo>Successfully ran all Order JSP tests!</echo>
    </target>

    <!-- Verify the edit order screen displays without errors -->
    <target name="EditOrder"
        description="Tests editing an existing Order's information">
        <webtest name="editOrder">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Order link" url="/editOrder.html?id=1"/>
                <verifytitle description="we should see the orderDetail title"
                    text=".*${orderDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a order and then save -->
    <target name="SaveOrder"
        description="Tests editing and saving a order">
        <webtest name="saveOrder">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Order link" url="/editOrder.html?id=1"/>
                <verifytitle description="we should see the orderDetail title"
                    text=".*${orderDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${orderDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Order -->
    <target name="AddOrder"
        description="Adds a new Order">
        <webtest name="addOrder">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOrder.html"/>
                <verifytitle description="we should see the orderDetail title"
                    text=".*${orderDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="Order List appears if save successful"
                    text=".*${orderList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${order.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing order -->
    <target name="DeleteOrder"
        description="Deletes existing Order">
        <webtest name="deleteOrder">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Order link" url="/editOrder.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Order List" text=".*${orderList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${order.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOrders"
        description="Tests search for and displaying all orders">
        <webtest name="searchOrders">
            &config;
            <steps>
                &login;
                <invoke description="click View Orders link" url="/orders.html"/>
                <verifytitle description="we should see the orderList title"
                    text=".*${orderList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--Order-END-->

<!--OrderCategory-START-->
    <!-- runs orderCategory-related tests -->
    <target name="OrderCategoryTests"
        depends="SearchOrderCategorys,EditOrderCategory,SaveOrderCategory,AddOrderCategory,DeleteOrderCategory"
        description="Call and executes all orderCategory test cases (targets)">
        <echo>Successfully ran all OrderCategory JSP tests!</echo>
    </target>

    <!-- Verify the edit orderCategory screen displays without errors -->
    <target name="EditOrderCategory"
        description="Tests editing an existing OrderCategory's information">
        <webtest name="editOrderCategory">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderCategory link" url="/editOrderCategory.html?id=1"/>
                <verifytitle description="we should see the orderCategoryDetail title"
                    text=".*${orderCategoryDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a orderCategory and then save -->
    <target name="SaveOrderCategory"
        description="Tests editing and saving a orderCategory">
        <webtest name="saveOrderCategory">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderCategory link" url="/editOrderCategory.html?id=1"/>
                <verifytitle description="we should see the orderCategoryDetail title"
                    text=".*${orderCategoryDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${orderCategoryDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OrderCategory -->
    <target name="AddOrderCategory"
        description="Adds a new OrderCategory">
        <webtest name="addOrderCategory">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOrderCategory.html"/>
                <verifytitle description="we should see the orderCategoryDetail title"
                    text=".*${orderCategoryDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OrderCategory List appears if save successful"
                    text=".*${orderCategoryList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderCategory.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing orderCategory -->
    <target name="DeleteOrderCategory"
        description="Deletes existing OrderCategory">
        <webtest name="deleteOrderCategory">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderCategory link" url="/editOrderCategory.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OrderCategory List" text=".*${orderCategoryList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderCategory.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOrderCategorys"
        description="Tests search for and displaying all orderCategorys">
        <webtest name="searchOrderCategorys">
            &config;
            <steps>
                &login;
                <invoke description="click View OrderCategorys link" url="/orderCategorys.html"/>
                <verifytitle description="we should see the orderCategoryList title"
                    text=".*${orderCategoryList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OrderCategory-END-->

<!--OrderDayInfo-START-->
    <!-- runs orderDayInfo-related tests -->
    <target name="OrderDayInfoTests"
        depends="SearchOrderDayInfos,EditOrderDayInfo,SaveOrderDayInfo,AddOrderDayInfo,DeleteOrderDayInfo"
        description="Call and executes all orderDayInfo test cases (targets)">
        <echo>Successfully ran all OrderDayInfo JSP tests!</echo>
    </target>

    <!-- Verify the edit orderDayInfo screen displays without errors -->
    <target name="EditOrderDayInfo"
        description="Tests editing an existing OrderDayInfo's information">
        <webtest name="editOrderDayInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderDayInfo link" url="/editOrderDayInfo.html?id=1"/>
                <verifytitle description="we should see the orderDayInfoDetail title"
                    text=".*${orderDayInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a orderDayInfo and then save -->
    <target name="SaveOrderDayInfo"
        description="Tests editing and saving a orderDayInfo">
        <webtest name="saveOrderDayInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderDayInfo link" url="/editOrderDayInfo.html?id=1"/>
                <verifytitle description="we should see the orderDayInfoDetail title"
                    text=".*${orderDayInfoDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${orderDayInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OrderDayInfo -->
    <target name="AddOrderDayInfo"
        description="Adds a new OrderDayInfo">
        <webtest name="addOrderDayInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOrderDayInfo.html"/>
                <verifytitle description="we should see the orderDayInfoDetail title"
                    text=".*${orderDayInfoDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OrderDayInfo List appears if save successful"
                    text=".*${orderDayInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderDayInfo.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing orderDayInfo -->
    <target name="DeleteOrderDayInfo"
        description="Deletes existing OrderDayInfo">
        <webtest name="deleteOrderDayInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderDayInfo link" url="/editOrderDayInfo.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OrderDayInfo List" text=".*${orderDayInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderDayInfo.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOrderDayInfos"
        description="Tests search for and displaying all orderDayInfos">
        <webtest name="searchOrderDayInfos">
            &config;
            <steps>
                &login;
                <invoke description="click View OrderDayInfos link" url="/orderDayInfos.html"/>
                <verifytitle description="we should see the orderDayInfoList title"
                    text=".*${orderDayInfoList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OrderDayInfo-END-->

<!--OrderDetail-START-->
    <!-- runs orderDetail-related tests -->
    <target name="OrderDetailTests"
        depends="SearchOrderDetails,EditOrderDetail,SaveOrderDetail,AddOrderDetail,DeleteOrderDetail"
        description="Call and executes all orderDetail test cases (targets)">
        <echo>Successfully ran all OrderDetail JSP tests!</echo>
    </target>

    <!-- Verify the edit orderDetail screen displays without errors -->
    <target name="EditOrderDetail"
        description="Tests editing an existing OrderDetail's information">
        <webtest name="editOrderDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderDetail link" url="/editOrderDetail.html?id=1"/>
                <verifytitle description="we should see the orderDetailDetail title"
                    text=".*${orderDetailDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a orderDetail and then save -->
    <target name="SaveOrderDetail"
        description="Tests editing and saving a orderDetail">
        <webtest name="saveOrderDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderDetail link" url="/editOrderDetail.html?id=1"/>
                <verifytitle description="we should see the orderDetailDetail title"
                    text=".*${orderDetailDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${orderDetailDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OrderDetail -->
    <target name="AddOrderDetail"
        description="Adds a new OrderDetail">
        <webtest name="addOrderDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOrderDetail.html"/>
                <verifytitle description="we should see the orderDetailDetail title"
                    text=".*${orderDetailDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OrderDetail List appears if save successful"
                    text=".*${orderDetailList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderDetail.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing orderDetail -->
    <target name="DeleteOrderDetail"
        description="Deletes existing OrderDetail">
        <webtest name="deleteOrderDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderDetail link" url="/editOrderDetail.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OrderDetail List" text=".*${orderDetailList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderDetail.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOrderDetails"
        description="Tests search for and displaying all orderDetails">
        <webtest name="searchOrderDetails">
            &config;
            <steps>
                &login;
                <invoke description="click View OrderDetails link" url="/orderDetails.html"/>
                <verifytitle description="we should see the orderDetailList title"
                    text=".*${orderDetailList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OrderDetail-END-->

<!--OrderLog-START-->
    <!-- runs orderLog-related tests -->
    <target name="OrderLogTests"
        depends="SearchOrderLogs,EditOrderLog,SaveOrderLog,AddOrderLog,DeleteOrderLog"
        description="Call and executes all orderLog test cases (targets)">
        <echo>Successfully ran all OrderLog JSP tests!</echo>
    </target>

    <!-- Verify the edit orderLog screen displays without errors -->
    <target name="EditOrderLog"
        description="Tests editing an existing OrderLog's information">
        <webtest name="editOrderLog">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderLog link" url="/editOrderLog.html?id=1"/>
                <verifytitle description="we should see the orderLogDetail title"
                    text=".*${orderLogDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a orderLog and then save -->
    <target name="SaveOrderLog"
        description="Tests editing and saving a orderLog">
        <webtest name="saveOrderLog">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderLog link" url="/editOrderLog.html?id=1"/>
                <verifytitle description="we should see the orderLogDetail title"
                    text=".*${orderLogDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${orderLogDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OrderLog -->
    <target name="AddOrderLog"
        description="Adds a new OrderLog">
        <webtest name="addOrderLog">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOrderLog.html"/>
                <verifytitle description="we should see the orderLogDetail title"
                    text=".*${orderLogDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OrderLog List appears if save successful"
                    text=".*${orderLogList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderLog.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing orderLog -->
    <target name="DeleteOrderLog"
        description="Deletes existing OrderLog">
        <webtest name="deleteOrderLog">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OrderLog link" url="/editOrderLog.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OrderLog List" text=".*${orderLogList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${orderLog.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOrderLogs"
        description="Tests search for and displaying all orderLogs">
        <webtest name="searchOrderLogs">
            &config;
            <steps>
                &login;
                <invoke description="click View OrderLogs link" url="/orderLogs.html"/>
                <verifytitle description="we should see the orderLogList title"
                    text=".*${orderLogList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OrderLog-END-->

<!--Contract-START-->
    <!-- runs contract-related tests -->
    <target name="ContractTests"
        depends="SearchContracts,EditContract,SaveContract,AddContract,DeleteContract"
        description="Call and executes all contract test cases (targets)">
        <echo>Successfully ran all Contract JSP tests!</echo>
    </target>

    <!-- Verify the edit contract screen displays without errors -->
    <target name="EditContract"
        description="Tests editing an existing Contract's information">
        <webtest name="editContract">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Contract link" url="/editContract.html?id=1"/>
                <verifytitle description="we should see the contractDetail title"
                    text=".*${contractDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a contract and then save -->
    <target name="SaveContract"
        description="Tests editing and saving a contract">
        <webtest name="saveContract">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Contract link" url="/editContract.html?id=1"/>
                <verifytitle description="we should see the contractDetail title"
                    text=".*${contractDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${contractDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Contract -->
    <target name="AddContract"
        description="Adds a new Contract">
        <webtest name="addContract">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editContract.html"/>
                <verifytitle description="we should see the contractDetail title"
                    text=".*${contractDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="Contract List appears if save successful"
                    text=".*${contractList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contract.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing contract -->
    <target name="DeleteContract"
        description="Deletes existing Contract">
        <webtest name="deleteContract">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Contract link" url="/editContract.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Contract List" text=".*${contractList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contract.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchContracts"
        description="Tests search for and displaying all contracts">
        <webtest name="searchContracts">
            &config;
            <steps>
                &login;
                <invoke description="click View Contracts link" url="/contracts.html"/>
                <verifytitle description="we should see the contractList title"
                    text=".*${contractList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--Contract-END-->













































<!--Org-START-->
    <!-- runs org-related tests -->
    <target name="OrgTests"
        depends="SearchOrgs,EditOrg,SaveOrg,AddOrg,DeleteOrg"
        description="Call and executes all org test cases (targets)">
        <echo>Successfully ran all Org JSP tests!</echo>
    </target>

    <!-- Verify the edit org screen displays without errors -->
    <target name="EditOrg"
        description="Tests editing an existing Org's information">
        <webtest name="editOrg">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Org link" url="/editOrg.html?id=1"/>
                <verifytitle description="we should see the orgDetail title"
                    text=".*${orgDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a org and then save -->
    <target name="SaveOrg"
        description="Tests editing and saving a org">
        <webtest name="saveOrg">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Org link" url="/editOrg.html?id=1"/>
                <verifytitle description="we should see the orgDetail title"
                    text=".*${orgDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${orgDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Org -->
    <target name="AddOrg"
        description="Adds a new Org">
        <webtest name="addOrg">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOrg.html"/>
                <verifytitle description="we should see the orgDetail title"
                    text=".*${orgDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="Org List appears if save successful"
                    text=".*${orgList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${org.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing org -->
    <target name="DeleteOrg"
        description="Deletes existing Org">
        <webtest name="deleteOrg">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Org link" url="/editOrg.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Org List" text=".*${orgList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${org.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOrgs"
        description="Tests search for and displaying all orgs">
        <webtest name="searchOrgs">
            &config;
            <steps>
                &login;
                <invoke description="click View Orgs link" url="/orgs.html"/>
                <verifytitle description="we should see the orgList title"
                    text=".*${orgList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--Org-END-->





<!--Branch-START-->
    <!-- runs branch-related tests -->
    <target name="BranchTests"
        depends="SearchBranchs,EditBranch,SaveBranch,AddBranch,DeleteBranch"
        description="Call and executes all branch test cases (targets)">
        <echo>Successfully ran all Branch JSP tests!</echo>
    </target>

    <!-- Verify the edit branch screen displays without errors -->
    <target name="EditBranch"
        description="Tests editing an existing Branch's information">
        <webtest name="editBranch">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Branch link" url="/editBranch.html?id=1"/>
                <verifytitle description="we should see the branchDetail title"
                    text=".*${branchDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a branch and then save -->
    <target name="SaveBranch"
        description="Tests editing and saving a branch">
        <webtest name="saveBranch">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Branch link" url="/editBranch.html?id=1"/>
                <verifytitle description="we should see the branchDetail title"
                    text=".*${branchDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set name" name="name" value="name"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${branchDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Branch -->
    <target name="AddBranch"
        description="Adds a new Branch">
        <webtest name="addBranch">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editBranch.html"/>
                <verifytitle description="we should see the branchDetail title"
                    text=".*${branchDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set name" name="name" value="LdHtYaHiEnMcUnGaCuQgYmCgZkMvZkTd"/>
                <setinputfield description="set displayNo" name="displayNo" value="1167026100"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="Branch List appears if save successful"
                    text=".*${branchList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${branch.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing branch -->
    <target name="DeleteBranch"
        description="Deletes existing Branch">
        <webtest name="deleteBranch">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Branch link" url="/editBranch.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Branch List" text=".*${branchList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${branch.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchBranchs"
        description="Tests search for and displaying all branchs">
        <webtest name="searchBranchs">
            &config;
            <steps>
                &login;
                <invoke description="click View Branchs link" url="/branchs.html"/>
                <verifytitle description="we should see the branchList title"
                    text=".*${branchList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--Branch-END-->

<!--ContractTarget-START-->
    <!-- runs contractTarget-related tests -->
    <target name="ContractTargetTests"
        depends="SearchContractTargets,EditContractTarget,SaveContractTarget,AddContractTarget,DeleteContractTarget"
        description="Call and executes all contractTarget test cases (targets)">
        <echo>Successfully ran all ContractTarget JSP tests!</echo>
    </target>

    <!-- Verify the edit contractTarget screen displays without errors -->
    <target name="EditContractTarget"
        description="Tests editing an existing ContractTarget's information">
        <webtest name="editContractTarget">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractTarget link" url="/editContractTarget.html?id=1"/>
                <verifytitle description="we should see the contractTargetDetail title"
                    text=".*${contractTargetDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a contractTarget and then save -->
    <target name="SaveContractTarget"
        description="Tests editing and saving a contractTarget">
        <webtest name="saveContractTarget">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractTarget link" url="/editContractTarget.html?id=1"/>
                <verifytitle description="we should see the contractTargetDetail title"
                    text=".*${contractTargetDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${contractTargetDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new ContractTarget -->
    <target name="AddContractTarget"
        description="Adds a new ContractTarget">
        <webtest name="addContractTarget">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editContractTarget.html"/>
                <verifytitle description="we should see the contractTargetDetail title"
                    text=".*${contractTargetDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="ContractTarget List appears if save successful"
                    text=".*${contractTargetList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contractTarget.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing contractTarget -->
    <target name="DeleteContractTarget"
        description="Deletes existing ContractTarget">
        <webtest name="deleteContractTarget">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractTarget link" url="/editContractTarget.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display ContractTarget List" text=".*${contractTargetList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contractTarget.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchContractTargets"
        description="Tests search for and displaying all contractTargets">
        <webtest name="searchContractTargets">
            &config;
            <steps>
                &login;
                <invoke description="click View ContractTargets link" url="/contractTargets.html"/>
                <verifytitle description="we should see the contractTargetList title"
                    text=".*${contractTargetList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--ContractTarget-END-->

<!--OaApplyInfo-START-->
    <!-- runs oaApplyInfo-related tests -->
    <target name="OaApplyInfoTests"
        depends="SearchOaApplyInfos,EditOaApplyInfo,SaveOaApplyInfo,AddOaApplyInfo,DeleteOaApplyInfo"
        description="Call and executes all oaApplyInfo test cases (targets)">
        <echo>Successfully ran all OaApplyInfo JSP tests!</echo>
    </target>

    <!-- Verify the edit oaApplyInfo screen displays without errors -->
    <target name="EditOaApplyInfo"
        description="Tests editing an existing OaApplyInfo's information">
        <webtest name="editOaApplyInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaApplyInfo link" url="/editOaApplyInfo.html?id=1"/>
                <verifytitle description="we should see the oaApplyInfoDetail title"
                    text=".*${oaApplyInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaApplyInfo and then save -->
    <target name="SaveOaApplyInfo"
        description="Tests editing and saving a oaApplyInfo">
        <webtest name="saveOaApplyInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaApplyInfo link" url="/editOaApplyInfo.html?id=1"/>
                <verifytitle description="we should see the oaApplyInfoDetail title"
                    text=".*${oaApplyInfoDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaApplyInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaApplyInfo -->
    <target name="AddOaApplyInfo"
        description="Adds a new OaApplyInfo">
        <webtest name="addOaApplyInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaApplyInfo.html"/>
                <verifytitle description="we should see the oaApplyInfoDetail title"
                    text=".*${oaApplyInfoDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaApplyInfo List appears if save successful"
                    text=".*${oaApplyInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaApplyInfo.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaApplyInfo -->
    <target name="DeleteOaApplyInfo"
        description="Deletes existing OaApplyInfo">
        <webtest name="deleteOaApplyInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaApplyInfo link" url="/editOaApplyInfo.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaApplyInfo List" text=".*${oaApplyInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaApplyInfo.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaApplyInfos"
        description="Tests search for and displaying all oaApplyInfos">
        <webtest name="searchOaApplyInfos">
            &config;
            <steps>
                &login;
                <invoke description="click View OaApplyInfos link" url="/oaApplyInfos.html"/>
                <verifytitle description="we should see the oaApplyInfoList title"
                    text=".*${oaApplyInfoList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaApplyInfo-END-->

<!--OaAreaCity-START-->
    <!-- runs oaAreaCity-related tests -->
    <target name="OaAreaCityTests"
        depends="SearchOaAreaCitys,EditOaAreaCity,SaveOaAreaCity,AddOaAreaCity,DeleteOaAreaCity"
        description="Call and executes all oaAreaCity test cases (targets)">
        <echo>Successfully ran all OaAreaCity JSP tests!</echo>
    </target>

    <!-- Verify the edit oaAreaCity screen displays without errors -->
    <target name="EditOaAreaCity"
        description="Tests editing an existing OaAreaCity's information">
        <webtest name="editOaAreaCity">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAreaCity link" url="/editOaAreaCity.html?id=1"/>
                <verifytitle description="we should see the oaAreaCityDetail title"
                    text=".*${oaAreaCityDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaAreaCity and then save -->
    <target name="SaveOaAreaCity"
        description="Tests editing and saving a oaAreaCity">
        <webtest name="saveOaAreaCity">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAreaCity link" url="/editOaAreaCity.html?id=1"/>
                <verifytitle description="we should see the oaAreaCityDetail title"
                    text=".*${oaAreaCityDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaAreaCityDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaAreaCity -->
    <target name="AddOaAreaCity"
        description="Adds a new OaAreaCity">
        <webtest name="addOaAreaCity">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaAreaCity.html"/>
                <verifytitle description="we should see the oaAreaCityDetail title"
                    text=".*${oaAreaCityDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaAreaCity List appears if save successful"
                    text=".*${oaAreaCityList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAreaCity.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaAreaCity -->
    <target name="DeleteOaAreaCity"
        description="Deletes existing OaAreaCity">
        <webtest name="deleteOaAreaCity">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAreaCity link" url="/editOaAreaCity.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaAreaCity List" text=".*${oaAreaCityList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAreaCity.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaAreaCitys"
        description="Tests search for and displaying all oaAreaCitys">
        <webtest name="searchOaAreaCitys">
            &config;
            <steps>
                &login;
                <invoke description="click View OaAreaCitys link" url="/oaAreaCitys.html"/>
                <verifytitle description="we should see the oaAreaCityList title"
                    text=".*${oaAreaCityList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaAreaCity-END-->

<!--OaAreaPc-START-->
    <!-- runs oaAreaPc-related tests -->
    <target name="OaAreaPcTests"
        depends="SearchOaAreaPcs,EditOaAreaPc,SaveOaAreaPc,AddOaAreaPc,DeleteOaAreaPc"
        description="Call and executes all oaAreaPc test cases (targets)">
        <echo>Successfully ran all OaAreaPc JSP tests!</echo>
    </target>

    <!-- Verify the edit oaAreaPc screen displays without errors -->
    <target name="EditOaAreaPc"
        description="Tests editing an existing OaAreaPc's information">
        <webtest name="editOaAreaPc">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAreaPc link" url="/editOaAreaPc.html?id=1"/>
                <verifytitle description="we should see the oaAreaPcDetail title"
                    text=".*${oaAreaPcDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaAreaPc and then save -->
    <target name="SaveOaAreaPc"
        description="Tests editing and saving a oaAreaPc">
        <webtest name="saveOaAreaPc">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAreaPc link" url="/editOaAreaPc.html?id=1"/>
                <verifytitle description="we should see the oaAreaPcDetail title"
                    text=".*${oaAreaPcDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaAreaPcDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaAreaPc -->
    <target name="AddOaAreaPc"
        description="Adds a new OaAreaPc">
        <webtest name="addOaAreaPc">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaAreaPc.html"/>
                <verifytitle description="we should see the oaAreaPcDetail title"
                    text=".*${oaAreaPcDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaAreaPc List appears if save successful"
                    text=".*${oaAreaPcList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAreaPc.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaAreaPc -->
    <target name="DeleteOaAreaPc"
        description="Deletes existing OaAreaPc">
        <webtest name="deleteOaAreaPc">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAreaPc link" url="/editOaAreaPc.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaAreaPc List" text=".*${oaAreaPcList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAreaPc.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaAreaPcs"
        description="Tests search for and displaying all oaAreaPcs">
        <webtest name="searchOaAreaPcs">
            &config;
            <steps>
                &login;
                <invoke description="click View OaAreaPcs link" url="/oaAreaPcs.html"/>
                <verifytitle description="we should see the oaAreaPcList title"
                    text=".*${oaAreaPcList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaAreaPc-END-->

<!--OaAssets-START-->
    <!-- runs oaAssets-related tests -->
    <target name="OaAssetsTests"
        depends="SearchOaAssetss,EditOaAssets,SaveOaAssets,AddOaAssets,DeleteOaAssets"
        description="Call and executes all oaAssets test cases (targets)">
        <echo>Successfully ran all OaAssets JSP tests!</echo>
    </target>

    <!-- Verify the edit oaAssets screen displays without errors -->
    <target name="EditOaAssets"
        description="Tests editing an existing OaAssets's information">
        <webtest name="editOaAssets">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssets link" url="/editOaAssets.html?id=1"/>
                <verifytitle description="we should see the oaAssetsDetail title"
                    text=".*${oaAssetsDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaAssets and then save -->
    <target name="SaveOaAssets"
        description="Tests editing and saving a oaAssets">
        <webtest name="saveOaAssets">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssets link" url="/editOaAssets.html?id=1"/>
                <verifytitle description="we should see the oaAssetsDetail title"
                    text=".*${oaAssetsDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaAssetsDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaAssets -->
    <target name="AddOaAssets"
        description="Adds a new OaAssets">
        <webtest name="addOaAssets">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaAssets.html"/>
                <verifytitle description="we should see the oaAssetsDetail title"
                    text=".*${oaAssetsDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaAssets List appears if save successful"
                    text=".*${oaAssetsList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAssets.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaAssets -->
    <target name="DeleteOaAssets"
        description="Deletes existing OaAssets">
        <webtest name="deleteOaAssets">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssets link" url="/editOaAssets.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaAssets List" text=".*${oaAssetsList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAssets.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaAssetss"
        description="Tests search for and displaying all oaAssetss">
        <webtest name="searchOaAssetss">
            &config;
            <steps>
                &login;
                <invoke description="click View OaAssetss link" url="/oaAssetss.html"/>
                <verifytitle description="we should see the oaAssetsList title"
                    text=".*${oaAssetsList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaAssets-END-->

<!--OaAssetsState-START-->
    <!-- runs oaAssetsState-related tests -->
    <target name="OaAssetsStateTests"
        depends="SearchOaAssetsStates,EditOaAssetsState,SaveOaAssetsState,AddOaAssetsState,DeleteOaAssetsState"
        description="Call and executes all oaAssetsState test cases (targets)">
        <echo>Successfully ran all OaAssetsState JSP tests!</echo>
    </target>

    <!-- Verify the edit oaAssetsState screen displays without errors -->
    <target name="EditOaAssetsState"
        description="Tests editing an existing OaAssetsState's information">
        <webtest name="editOaAssetsState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssetsState link" url="/editOaAssetsState.html?id=1"/>
                <verifytitle description="we should see the oaAssetsStateDetail title"
                    text=".*${oaAssetsStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaAssetsState and then save -->
    <target name="SaveOaAssetsState"
        description="Tests editing and saving a oaAssetsState">
        <webtest name="saveOaAssetsState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssetsState link" url="/editOaAssetsState.html?id=1"/>
                <verifytitle description="we should see the oaAssetsStateDetail title"
                    text=".*${oaAssetsStateDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaAssetsStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaAssetsState -->
    <target name="AddOaAssetsState"
        description="Adds a new OaAssetsState">
        <webtest name="addOaAssetsState">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaAssetsState.html"/>
                <verifytitle description="we should see the oaAssetsStateDetail title"
                    text=".*${oaAssetsStateDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaAssetsState List appears if save successful"
                    text=".*${oaAssetsStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAssetsState.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaAssetsState -->
    <target name="DeleteOaAssetsState"
        description="Deletes existing OaAssetsState">
        <webtest name="deleteOaAssetsState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssetsState link" url="/editOaAssetsState.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaAssetsState List" text=".*${oaAssetsStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAssetsState.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaAssetsStates"
        description="Tests search for and displaying all oaAssetsStates">
        <webtest name="searchOaAssetsStates">
            &config;
            <steps>
                &login;
                <invoke description="click View OaAssetsStates link" url="/oaAssetsStates.html"/>
                <verifytitle description="we should see the oaAssetsStateList title"
                    text=".*${oaAssetsStateList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaAssetsState-END-->

<!--OaAssetsType-START-->
    <!-- runs oaAssetsType-related tests -->
    <target name="OaAssetsTypeTests"
        depends="SearchOaAssetsTypes,EditOaAssetsType,SaveOaAssetsType,AddOaAssetsType,DeleteOaAssetsType"
        description="Call and executes all oaAssetsType test cases (targets)">
        <echo>Successfully ran all OaAssetsType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaAssetsType screen displays without errors -->
    <target name="EditOaAssetsType"
        description="Tests editing an existing OaAssetsType's information">
        <webtest name="editOaAssetsType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssetsType link" url="/editOaAssetsType.html?id=1"/>
                <verifytitle description="we should see the oaAssetsTypeDetail title"
                    text=".*${oaAssetsTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaAssetsType and then save -->
    <target name="SaveOaAssetsType"
        description="Tests editing and saving a oaAssetsType">
        <webtest name="saveOaAssetsType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssetsType link" url="/editOaAssetsType.html?id=1"/>
                <verifytitle description="we should see the oaAssetsTypeDetail title"
                    text=".*${oaAssetsTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaAssetsTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaAssetsType -->
    <target name="AddOaAssetsType"
        description="Adds a new OaAssetsType">
        <webtest name="addOaAssetsType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaAssetsType.html"/>
                <verifytitle description="we should see the oaAssetsTypeDetail title"
                    text=".*${oaAssetsTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaAssetsType List appears if save successful"
                    text=".*${oaAssetsTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAssetsType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaAssetsType -->
    <target name="DeleteOaAssetsType"
        description="Deletes existing OaAssetsType">
        <webtest name="deleteOaAssetsType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaAssetsType link" url="/editOaAssetsType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaAssetsType List" text=".*${oaAssetsTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaAssetsType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaAssetsTypes"
        description="Tests search for and displaying all oaAssetsTypes">
        <webtest name="searchOaAssetsTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaAssetsTypes link" url="/oaAssetsTypes.html"/>
                <verifytitle description="we should see the oaAssetsTypeList title"
                    text=".*${oaAssetsTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaAssetsType-END-->

<!--OaBusinessCard-START-->
    <!-- runs oaBusinessCard-related tests -->
    <target name="OaBusinessCardTests"
        depends="SearchOaBusinessCards,EditOaBusinessCard,SaveOaBusinessCard,AddOaBusinessCard,DeleteOaBusinessCard"
        description="Call and executes all oaBusinessCard test cases (targets)">
        <echo>Successfully ran all OaBusinessCard JSP tests!</echo>
    </target>

    <!-- Verify the edit oaBusinessCard screen displays without errors -->
    <target name="EditOaBusinessCard"
        description="Tests editing an existing OaBusinessCard's information">
        <webtest name="editOaBusinessCard">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaBusinessCard link" url="/editOaBusinessCard.html?id=1"/>
                <verifytitle description="we should see the oaBusinessCardDetail title"
                    text=".*${oaBusinessCardDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaBusinessCard and then save -->
    <target name="SaveOaBusinessCard"
        description="Tests editing and saving a oaBusinessCard">
        <webtest name="saveOaBusinessCard">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaBusinessCard link" url="/editOaBusinessCard.html?id=1"/>
                <verifytitle description="we should see the oaBusinessCardDetail title"
                    text=".*${oaBusinessCardDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaBusinessCardDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaBusinessCard -->
    <target name="AddOaBusinessCard"
        description="Adds a new OaBusinessCard">
        <webtest name="addOaBusinessCard">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaBusinessCard.html"/>
                <verifytitle description="we should see the oaBusinessCardDetail title"
                    text=".*${oaBusinessCardDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaBusinessCard List appears if save successful"
                    text=".*${oaBusinessCardList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaBusinessCard.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaBusinessCard -->
    <target name="DeleteOaBusinessCard"
        description="Deletes existing OaBusinessCard">
        <webtest name="deleteOaBusinessCard">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaBusinessCard link" url="/editOaBusinessCard.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaBusinessCard List" text=".*${oaBusinessCardList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaBusinessCard.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaBusinessCards"
        description="Tests search for and displaying all oaBusinessCards">
        <webtest name="searchOaBusinessCards">
            &config;
            <steps>
                &login;
                <invoke description="click View OaBusinessCards link" url="/oaBusinessCards.html"/>
                <verifytitle description="we should see the oaBusinessCardList title"
                    text=".*${oaBusinessCardList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaBusinessCard-END-->

<!--OaBusinessCardType-START-->
    <!-- runs oaBusinessCardType-related tests -->
    <target name="OaBusinessCardTypeTests"
        depends="SearchOaBusinessCardTypes,EditOaBusinessCardType,SaveOaBusinessCardType,AddOaBusinessCardType,DeleteOaBusinessCardType"
        description="Call and executes all oaBusinessCardType test cases (targets)">
        <echo>Successfully ran all OaBusinessCardType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaBusinessCardType screen displays without errors -->
    <target name="EditOaBusinessCardType"
        description="Tests editing an existing OaBusinessCardType's information">
        <webtest name="editOaBusinessCardType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaBusinessCardType link" url="/editOaBusinessCardType.html?id=1"/>
                <verifytitle description="we should see the oaBusinessCardTypeDetail title"
                    text=".*${oaBusinessCardTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaBusinessCardType and then save -->
    <target name="SaveOaBusinessCardType"
        description="Tests editing and saving a oaBusinessCardType">
        <webtest name="saveOaBusinessCardType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaBusinessCardType link" url="/editOaBusinessCardType.html?id=1"/>
                <verifytitle description="we should see the oaBusinessCardTypeDetail title"
                    text=".*${oaBusinessCardTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaBusinessCardTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaBusinessCardType -->
    <target name="AddOaBusinessCardType"
        description="Adds a new OaBusinessCardType">
        <webtest name="addOaBusinessCardType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaBusinessCardType.html"/>
                <verifytitle description="we should see the oaBusinessCardTypeDetail title"
                    text=".*${oaBusinessCardTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaBusinessCardType List appears if save successful"
                    text=".*${oaBusinessCardTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaBusinessCardType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaBusinessCardType -->
    <target name="DeleteOaBusinessCardType"
        description="Deletes existing OaBusinessCardType">
        <webtest name="deleteOaBusinessCardType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaBusinessCardType link" url="/editOaBusinessCardType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaBusinessCardType List" text=".*${oaBusinessCardTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaBusinessCardType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaBusinessCardTypes"
        description="Tests search for and displaying all oaBusinessCardTypes">
        <webtest name="searchOaBusinessCardTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaBusinessCardTypes link" url="/oaBusinessCardTypes.html"/>
                <verifytitle description="we should see the oaBusinessCardTypeList title"
                    text=".*${oaBusinessCardTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaBusinessCardType-END-->

<!--OaCalendarEvent-START-->
    <!-- runs oaCalendarEvent-related tests -->
    <target name="OaCalendarEventTests"
        depends="SearchOaCalendarEvents,EditOaCalendarEvent,SaveOaCalendarEvent,AddOaCalendarEvent,DeleteOaCalendarEvent"
        description="Call and executes all oaCalendarEvent test cases (targets)">
        <echo>Successfully ran all OaCalendarEvent JSP tests!</echo>
    </target>

    <!-- Verify the edit oaCalendarEvent screen displays without errors -->
    <target name="EditOaCalendarEvent"
        description="Tests editing an existing OaCalendarEvent's information">
        <webtest name="editOaCalendarEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaCalendarEvent link" url="/editOaCalendarEvent.html?id=1"/>
                <verifytitle description="we should see the oaCalendarEventDetail title"
                    text=".*${oaCalendarEventDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaCalendarEvent and then save -->
    <target name="SaveOaCalendarEvent"
        description="Tests editing and saving a oaCalendarEvent">
        <webtest name="saveOaCalendarEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaCalendarEvent link" url="/editOaCalendarEvent.html?id=1"/>
                <verifytitle description="we should see the oaCalendarEventDetail title"
                    text=".*${oaCalendarEventDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaCalendarEventDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaCalendarEvent -->
    <target name="AddOaCalendarEvent"
        description="Adds a new OaCalendarEvent">
        <webtest name="addOaCalendarEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaCalendarEvent.html"/>
                <verifytitle description="we should see the oaCalendarEventDetail title"
                    text=".*${oaCalendarEventDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaCalendarEvent List appears if save successful"
                    text=".*${oaCalendarEventList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaCalendarEvent.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaCalendarEvent -->
    <target name="DeleteOaCalendarEvent"
        description="Deletes existing OaCalendarEvent">
        <webtest name="deleteOaCalendarEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaCalendarEvent link" url="/editOaCalendarEvent.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaCalendarEvent List" text=".*${oaCalendarEventList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaCalendarEvent.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaCalendarEvents"
        description="Tests search for and displaying all oaCalendarEvents">
        <webtest name="searchOaCalendarEvents">
            &config;
            <steps>
                &login;
                <invoke description="click View OaCalendarEvents link" url="/oaCalendarEvents.html"/>
                <verifytitle description="we should see the oaCalendarEventList title"
                    text=".*${oaCalendarEventList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaCalendarEvent-END-->

<!--OaDocument-START-->
    <!-- runs oaDocument-related tests -->
    <target name="OaDocumentTests"
        depends="SearchOaDocuments,EditOaDocument,SaveOaDocument,AddOaDocument,DeleteOaDocument"
        description="Call and executes all oaDocument test cases (targets)">
        <echo>Successfully ran all OaDocument JSP tests!</echo>
    </target>

    <!-- Verify the edit oaDocument screen displays without errors -->
    <target name="EditOaDocument"
        description="Tests editing an existing OaDocument's information">
        <webtest name="editOaDocument">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocument link" url="/editOaDocument.html?id=1"/>
                <verifytitle description="we should see the oaDocumentDetail title"
                    text=".*${oaDocumentDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaDocument and then save -->
    <target name="SaveOaDocument"
        description="Tests editing and saving a oaDocument">
        <webtest name="saveOaDocument">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocument link" url="/editOaDocument.html?id=1"/>
                <verifytitle description="we should see the oaDocumentDetail title"
                    text=".*${oaDocumentDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaDocumentDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaDocument -->
    <target name="AddOaDocument"
        description="Adds a new OaDocument">
        <webtest name="addOaDocument">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaDocument.html"/>
                <verifytitle description="we should see the oaDocumentDetail title"
                    text=".*${oaDocumentDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaDocument List appears if save successful"
                    text=".*${oaDocumentList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocument.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaDocument -->
    <target name="DeleteOaDocument"
        description="Deletes existing OaDocument">
        <webtest name="deleteOaDocument">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocument link" url="/editOaDocument.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaDocument List" text=".*${oaDocumentList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocument.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaDocuments"
        description="Tests search for and displaying all oaDocuments">
        <webtest name="searchOaDocuments">
            &config;
            <steps>
                &login;
                <invoke description="click View OaDocuments link" url="/oaDocuments.html"/>
                <verifytitle description="we should see the oaDocumentList title"
                    text=".*${oaDocumentList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaDocument-END-->

<!--OaDocumentCatalog-START-->
    <!-- runs oaDocumentCatalog-related tests -->
    <target name="OaDocumentCatalogTests"
        depends="SearchOaDocumentCatalogs,EditOaDocumentCatalog,SaveOaDocumentCatalog,AddOaDocumentCatalog,DeleteOaDocumentCatalog"
        description="Call and executes all oaDocumentCatalog test cases (targets)">
        <echo>Successfully ran all OaDocumentCatalog JSP tests!</echo>
    </target>

    <!-- Verify the edit oaDocumentCatalog screen displays without errors -->
    <target name="EditOaDocumentCatalog"
        description="Tests editing an existing OaDocumentCatalog's information">
        <webtest name="editOaDocumentCatalog">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentCatalog link" url="/editOaDocumentCatalog.html?id=1"/>
                <verifytitle description="we should see the oaDocumentCatalogDetail title"
                    text=".*${oaDocumentCatalogDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaDocumentCatalog and then save -->
    <target name="SaveOaDocumentCatalog"
        description="Tests editing and saving a oaDocumentCatalog">
        <webtest name="saveOaDocumentCatalog">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentCatalog link" url="/editOaDocumentCatalog.html?id=1"/>
                <verifytitle description="we should see the oaDocumentCatalogDetail title"
                    text=".*${oaDocumentCatalogDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaDocumentCatalogDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaDocumentCatalog -->
    <target name="AddOaDocumentCatalog"
        description="Adds a new OaDocumentCatalog">
        <webtest name="addOaDocumentCatalog">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaDocumentCatalog.html"/>
                <verifytitle description="we should see the oaDocumentCatalogDetail title"
                    text=".*${oaDocumentCatalogDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaDocumentCatalog List appears if save successful"
                    text=".*${oaDocumentCatalogList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocumentCatalog.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaDocumentCatalog -->
    <target name="DeleteOaDocumentCatalog"
        description="Deletes existing OaDocumentCatalog">
        <webtest name="deleteOaDocumentCatalog">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentCatalog link" url="/editOaDocumentCatalog.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaDocumentCatalog List" text=".*${oaDocumentCatalogList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocumentCatalog.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaDocumentCatalogs"
        description="Tests search for and displaying all oaDocumentCatalogs">
        <webtest name="searchOaDocumentCatalogs">
            &config;
            <steps>
                &login;
                <invoke description="click View OaDocumentCatalogs link" url="/oaDocumentCatalogs.html"/>
                <verifytitle description="we should see the oaDocumentCatalogList title"
                    text=".*${oaDocumentCatalogList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaDocumentCatalog-END-->

<!--OaDocumentCatalogPermitType-START-->
    <!-- runs oaDocumentCatalogPermitType-related tests -->
    <target name="OaDocumentCatalogPermitTypeTests"
        depends="SearchOaDocumentCatalogPermitTypes,EditOaDocumentCatalogPermitType,SaveOaDocumentCatalogPermitType,AddOaDocumentCatalogPermitType,DeleteOaDocumentCatalogPermitType"
        description="Call and executes all oaDocumentCatalogPermitType test cases (targets)">
        <echo>Successfully ran all OaDocumentCatalogPermitType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaDocumentCatalogPermitType screen displays without errors -->
    <target name="EditOaDocumentCatalogPermitType"
        description="Tests editing an existing OaDocumentCatalogPermitType's information">
        <webtest name="editOaDocumentCatalogPermitType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentCatalogPermitType link" url="/editOaDocumentCatalogPermitType.html?id=1"/>
                <verifytitle description="we should see the oaDocumentCatalogPermitTypeDetail title"
                    text=".*${oaDocumentCatalogPermitTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaDocumentCatalogPermitType and then save -->
    <target name="SaveOaDocumentCatalogPermitType"
        description="Tests editing and saving a oaDocumentCatalogPermitType">
        <webtest name="saveOaDocumentCatalogPermitType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentCatalogPermitType link" url="/editOaDocumentCatalogPermitType.html?id=1"/>
                <verifytitle description="we should see the oaDocumentCatalogPermitTypeDetail title"
                    text=".*${oaDocumentCatalogPermitTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaDocumentCatalogPermitTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaDocumentCatalogPermitType -->
    <target name="AddOaDocumentCatalogPermitType"
        description="Adds a new OaDocumentCatalogPermitType">
        <webtest name="addOaDocumentCatalogPermitType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaDocumentCatalogPermitType.html"/>
                <verifytitle description="we should see the oaDocumentCatalogPermitTypeDetail title"
                    text=".*${oaDocumentCatalogPermitTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaDocumentCatalogPermitType List appears if save successful"
                    text=".*${oaDocumentCatalogPermitTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocumentCatalogPermitType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaDocumentCatalogPermitType -->
    <target name="DeleteOaDocumentCatalogPermitType"
        description="Deletes existing OaDocumentCatalogPermitType">
        <webtest name="deleteOaDocumentCatalogPermitType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentCatalogPermitType link" url="/editOaDocumentCatalogPermitType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaDocumentCatalogPermitType List" text=".*${oaDocumentCatalogPermitTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocumentCatalogPermitType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaDocumentCatalogPermitTypes"
        description="Tests search for and displaying all oaDocumentCatalogPermitTypes">
        <webtest name="searchOaDocumentCatalogPermitTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaDocumentCatalogPermitTypes link" url="/oaDocumentCatalogPermitTypes.html"/>
                <verifytitle description="we should see the oaDocumentCatalogPermitTypeList title"
                    text=".*${oaDocumentCatalogPermitTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaDocumentCatalogPermitType-END-->

<!--OaDocumentFile-START-->
    <!-- runs oaDocumentFile-related tests -->
    <target name="OaDocumentFileTests"
        depends="SearchOaDocumentFiles,EditOaDocumentFile,SaveOaDocumentFile,AddOaDocumentFile,DeleteOaDocumentFile"
        description="Call and executes all oaDocumentFile test cases (targets)">
        <echo>Successfully ran all OaDocumentFile JSP tests!</echo>
    </target>

    <!-- Verify the edit oaDocumentFile screen displays without errors -->
    <target name="EditOaDocumentFile"
        description="Tests editing an existing OaDocumentFile's information">
        <webtest name="editOaDocumentFile">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentFile link" url="/editOaDocumentFile.html?id=1"/>
                <verifytitle description="we should see the oaDocumentFileDetail title"
                    text=".*${oaDocumentFileDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaDocumentFile and then save -->
    <target name="SaveOaDocumentFile"
        description="Tests editing and saving a oaDocumentFile">
        <webtest name="saveOaDocumentFile">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentFile link" url="/editOaDocumentFile.html?id=1"/>
                <verifytitle description="we should see the oaDocumentFileDetail title"
                    text=".*${oaDocumentFileDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaDocumentFileDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaDocumentFile -->
    <target name="AddOaDocumentFile"
        description="Adds a new OaDocumentFile">
        <webtest name="addOaDocumentFile">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaDocumentFile.html"/>
                <verifytitle description="we should see the oaDocumentFileDetail title"
                    text=".*${oaDocumentFileDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaDocumentFile List appears if save successful"
                    text=".*${oaDocumentFileList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocumentFile.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaDocumentFile -->
    <target name="DeleteOaDocumentFile"
        description="Deletes existing OaDocumentFile">
        <webtest name="deleteOaDocumentFile">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaDocumentFile link" url="/editOaDocumentFile.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaDocumentFile List" text=".*${oaDocumentFileList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaDocumentFile.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaDocumentFiles"
        description="Tests search for and displaying all oaDocumentFiles">
        <webtest name="searchOaDocumentFiles">
            &config;
            <steps>
                &login;
                <invoke description="click View OaDocumentFiles link" url="/oaDocumentFiles.html"/>
                <verifytitle description="we should see the oaDocumentFileList title"
                    text=".*${oaDocumentFileList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaDocumentFile-END-->

<!--OaInfo-START-->
    <!-- runs oaInfo-related tests -->
    <target name="OaInfoTests"
        depends="SearchOaInfos,EditOaInfo,SaveOaInfo,AddOaInfo,DeleteOaInfo"
        description="Call and executes all oaInfo test cases (targets)">
        <echo>Successfully ran all OaInfo JSP tests!</echo>
    </target>

    <!-- Verify the edit oaInfo screen displays without errors -->
    <target name="EditOaInfo"
        description="Tests editing an existing OaInfo's information">
        <webtest name="editOaInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaInfo link" url="/editOaInfo.html?id=1"/>
                <verifytitle description="we should see the oaInfoDetail title"
                    text=".*${oaInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaInfo and then save -->
    <target name="SaveOaInfo"
        description="Tests editing and saving a oaInfo">
        <webtest name="saveOaInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaInfo link" url="/editOaInfo.html?id=1"/>
                <verifytitle description="we should see the oaInfoDetail title"
                    text=".*${oaInfoDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaInfo -->
    <target name="AddOaInfo"
        description="Adds a new OaInfo">
        <webtest name="addOaInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaInfo.html"/>
                <verifytitle description="we should see the oaInfoDetail title"
                    text=".*${oaInfoDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaInfo List appears if save successful"
                    text=".*${oaInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaInfo.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaInfo -->
    <target name="DeleteOaInfo"
        description="Deletes existing OaInfo">
        <webtest name="deleteOaInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaInfo link" url="/editOaInfo.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaInfo List" text=".*${oaInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaInfo.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaInfos"
        description="Tests search for and displaying all oaInfos">
        <webtest name="searchOaInfos">
            &config;
            <steps>
                &login;
                <invoke description="click View OaInfos link" url="/oaInfos.html"/>
                <verifytitle description="we should see the oaInfoList title"
                    text=".*${oaInfoList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaInfo-END-->

<!--OaInfoType-START-->
    <!-- runs oaInfoType-related tests -->
    <target name="OaInfoTypeTests"
        depends="SearchOaInfoTypes,EditOaInfoType,SaveOaInfoType,AddOaInfoType,DeleteOaInfoType"
        description="Call and executes all oaInfoType test cases (targets)">
        <echo>Successfully ran all OaInfoType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaInfoType screen displays without errors -->
    <target name="EditOaInfoType"
        description="Tests editing an existing OaInfoType's information">
        <webtest name="editOaInfoType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaInfoType link" url="/editOaInfoType.html?id=1"/>
                <verifytitle description="we should see the oaInfoTypeDetail title"
                    text=".*${oaInfoTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaInfoType and then save -->
    <target name="SaveOaInfoType"
        description="Tests editing and saving a oaInfoType">
        <webtest name="saveOaInfoType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaInfoType link" url="/editOaInfoType.html?id=1"/>
                <verifytitle description="we should see the oaInfoTypeDetail title"
                    text=".*${oaInfoTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaInfoTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaInfoType -->
    <target name="AddOaInfoType"
        description="Adds a new OaInfoType">
        <webtest name="addOaInfoType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaInfoType.html"/>
                <verifytitle description="we should see the oaInfoTypeDetail title"
                    text=".*${oaInfoTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaInfoType List appears if save successful"
                    text=".*${oaInfoTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaInfoType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaInfoType -->
    <target name="DeleteOaInfoType"
        description="Deletes existing OaInfoType">
        <webtest name="deleteOaInfoType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaInfoType link" url="/editOaInfoType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaInfoType List" text=".*${oaInfoTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaInfoType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaInfoTypes"
        description="Tests search for and displaying all oaInfoTypes">
        <webtest name="searchOaInfoTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaInfoTypes link" url="/oaInfoTypes.html"/>
                <verifytitle description="we should see the oaInfoTypeList title"
                    text=".*${oaInfoTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaInfoType-END-->

<!--OaProductInfo-START-->
    <!-- runs oaProductInfo-related tests -->
    <target name="OaProductInfoTests"
        depends="SearchOaProductInfos,EditOaProductInfo,SaveOaProductInfo,AddOaProductInfo,DeleteOaProductInfo"
        description="Call and executes all oaProductInfo test cases (targets)">
        <echo>Successfully ran all OaProductInfo JSP tests!</echo>
    </target>

    <!-- Verify the edit oaProductInfo screen displays without errors -->
    <target name="EditOaProductInfo"
        description="Tests editing an existing OaProductInfo's information">
        <webtest name="editOaProductInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductInfo link" url="/editOaProductInfo.html?id=1"/>
                <verifytitle description="we should see the oaProductInfoDetail title"
                    text=".*${oaProductInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaProductInfo and then save -->
    <target name="SaveOaProductInfo"
        description="Tests editing and saving a oaProductInfo">
        <webtest name="saveOaProductInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductInfo link" url="/editOaProductInfo.html?id=1"/>
                <verifytitle description="we should see the oaProductInfoDetail title"
                    text=".*${oaProductInfoDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaProductInfoDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaProductInfo -->
    <target name="AddOaProductInfo"
        description="Adds a new OaProductInfo">
        <webtest name="addOaProductInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaProductInfo.html"/>
                <verifytitle description="we should see the oaProductInfoDetail title"
                    text=".*${oaProductInfoDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaProductInfo List appears if save successful"
                    text=".*${oaProductInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaProductInfo.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaProductInfo -->
    <target name="DeleteOaProductInfo"
        description="Deletes existing OaProductInfo">
        <webtest name="deleteOaProductInfo">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductInfo link" url="/editOaProductInfo.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaProductInfo List" text=".*${oaProductInfoList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaProductInfo.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaProductInfos"
        description="Tests search for and displaying all oaProductInfos">
        <webtest name="searchOaProductInfos">
            &config;
            <steps>
                &login;
                <invoke description="click View OaProductInfos link" url="/oaProductInfos.html"/>
                <verifytitle description="we should see the oaProductInfoList title"
                    text=".*${oaProductInfoList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaProductInfo-END-->

<!--OaProductType-START-->
    <!-- runs oaProductType-related tests -->
    <target name="OaProductTypeTests"
        depends="SearchOaProductTypes,EditOaProductType,SaveOaProductType,AddOaProductType,DeleteOaProductType"
        description="Call and executes all oaProductType test cases (targets)">
        <echo>Successfully ran all OaProductType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaProductType screen displays without errors -->
    <target name="EditOaProductType"
        description="Tests editing an existing OaProductType's information">
        <webtest name="editOaProductType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductType link" url="/editOaProductType.html?id=1"/>
                <verifytitle description="we should see the oaProductTypeDetail title"
                    text=".*${oaProductTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaProductType and then save -->
    <target name="SaveOaProductType"
        description="Tests editing and saving a oaProductType">
        <webtest name="saveOaProductType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductType link" url="/editOaProductType.html?id=1"/>
                <verifytitle description="we should see the oaProductTypeDetail title"
                    text=".*${oaProductTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaProductTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaProductType -->
    <target name="AddOaProductType"
        description="Adds a new OaProductType">
        <webtest name="addOaProductType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaProductType.html"/>
                <verifytitle description="we should see the oaProductTypeDetail title"
                    text=".*${oaProductTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaProductType List appears if save successful"
                    text=".*${oaProductTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaProductType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaProductType -->
    <target name="DeleteOaProductType"
        description="Deletes existing OaProductType">
        <webtest name="deleteOaProductType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductType link" url="/editOaProductType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaProductType List" text=".*${oaProductTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaProductType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaProductTypes"
        description="Tests search for and displaying all oaProductTypes">
        <webtest name="searchOaProductTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaProductTypes link" url="/oaProductTypes.html"/>
                <verifytitle description="we should see the oaProductTypeList title"
                    text=".*${oaProductTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaProductType-END-->

<!--OaProductUsed-START-->
    <!-- runs oaProductUsed-related tests -->
    <target name="OaProductUsedTests"
        depends="SearchOaProductUseds,EditOaProductUsed,SaveOaProductUsed,AddOaProductUsed,DeleteOaProductUsed"
        description="Call and executes all oaProductUsed test cases (targets)">
        <echo>Successfully ran all OaProductUsed JSP tests!</echo>
    </target>

    <!-- Verify the edit oaProductUsed screen displays without errors -->
    <target name="EditOaProductUsed"
        description="Tests editing an existing OaProductUsed's information">
        <webtest name="editOaProductUsed">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductUsed link" url="/editOaProductUsed.html?id=1"/>
                <verifytitle description="we should see the oaProductUsedDetail title"
                    text=".*${oaProductUsedDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaProductUsed and then save -->
    <target name="SaveOaProductUsed"
        description="Tests editing and saving a oaProductUsed">
        <webtest name="saveOaProductUsed">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductUsed link" url="/editOaProductUsed.html?id=1"/>
                <verifytitle description="we should see the oaProductUsedDetail title"
                    text=".*${oaProductUsedDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaProductUsedDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaProductUsed -->
    <target name="AddOaProductUsed"
        description="Adds a new OaProductUsed">
        <webtest name="addOaProductUsed">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaProductUsed.html"/>
                <verifytitle description="we should see the oaProductUsedDetail title"
                    text=".*${oaProductUsedDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaProductUsed List appears if save successful"
                    text=".*${oaProductUsedList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaProductUsed.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaProductUsed -->
    <target name="DeleteOaProductUsed"
        description="Deletes existing OaProductUsed">
        <webtest name="deleteOaProductUsed">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaProductUsed link" url="/editOaProductUsed.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaProductUsed List" text=".*${oaProductUsedList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaProductUsed.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaProductUseds"
        description="Tests search for and displaying all oaProductUseds">
        <webtest name="searchOaProductUseds">
            &config;
            <steps>
                &login;
                <invoke description="click View OaProductUseds link" url="/oaProductUseds.html"/>
                <verifytitle description="we should see the oaProductUsedList title"
                    text=".*${oaProductUsedList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaProductUsed-END-->

<!--OaScratchpad-START-->
    <!-- runs oaScratchpad-related tests -->
    <target name="OaScratchpadTests"
        depends="SearchOaScratchpads,EditOaScratchpad,SaveOaScratchpad,AddOaScratchpad,DeleteOaScratchpad"
        description="Call and executes all oaScratchpad test cases (targets)">
        <echo>Successfully ran all OaScratchpad JSP tests!</echo>
    </target>

    <!-- Verify the edit oaScratchpad screen displays without errors -->
    <target name="EditOaScratchpad"
        description="Tests editing an existing OaScratchpad's information">
        <webtest name="editOaScratchpad">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaScratchpad link" url="/editOaScratchpad.html?id=1"/>
                <verifytitle description="we should see the oaScratchpadDetail title"
                    text=".*${oaScratchpadDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaScratchpad and then save -->
    <target name="SaveOaScratchpad"
        description="Tests editing and saving a oaScratchpad">
        <webtest name="saveOaScratchpad">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaScratchpad link" url="/editOaScratchpad.html?id=1"/>
                <verifytitle description="we should see the oaScratchpadDetail title"
                    text=".*${oaScratchpadDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaScratchpadDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaScratchpad -->
    <target name="AddOaScratchpad"
        description="Adds a new OaScratchpad">
        <webtest name="addOaScratchpad">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaScratchpad.html"/>
                <verifytitle description="we should see the oaScratchpadDetail title"
                    text=".*${oaScratchpadDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaScratchpad List appears if save successful"
                    text=".*${oaScratchpadList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaScratchpad.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaScratchpad -->
    <target name="DeleteOaScratchpad"
        description="Deletes existing OaScratchpad">
        <webtest name="deleteOaScratchpad">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaScratchpad link" url="/editOaScratchpad.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaScratchpad List" text=".*${oaScratchpadList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaScratchpad.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaScratchpads"
        description="Tests search for and displaying all oaScratchpads">
        <webtest name="searchOaScratchpads">
            &config;
            <steps>
                &login;
                <invoke description="click View OaScratchpads link" url="/oaScratchpads.html"/>
                <verifytitle description="we should see the oaScratchpadList title"
                    text=".*${oaScratchpadList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaScratchpad-END-->

<!--OaWorkFlow-START-->
    <!-- runs oaWorkFlow-related tests -->
    <target name="OaWorkFlowTests"
        depends="SearchOaWorkFlows,EditOaWorkFlow,SaveOaWorkFlow,AddOaWorkFlow,DeleteOaWorkFlow"
        description="Call and executes all oaWorkFlow test cases (targets)">
        <echo>Successfully ran all OaWorkFlow JSP tests!</echo>
    </target>

    <!-- Verify the edit oaWorkFlow screen displays without errors -->
    <target name="EditOaWorkFlow"
        description="Tests editing an existing OaWorkFlow's information">
        <webtest name="editOaWorkFlow">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlow link" url="/editOaWorkFlow.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowDetail title"
                    text=".*${oaWorkFlowDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaWorkFlow and then save -->
    <target name="SaveOaWorkFlow"
        description="Tests editing and saving a oaWorkFlow">
        <webtest name="saveOaWorkFlow">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlow link" url="/editOaWorkFlow.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowDetail title"
                    text=".*${oaWorkFlowDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaWorkFlowDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaWorkFlow -->
    <target name="AddOaWorkFlow"
        description="Adds a new OaWorkFlow">
        <webtest name="addOaWorkFlow">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaWorkFlow.html"/>
                <verifytitle description="we should see the oaWorkFlowDetail title"
                    text=".*${oaWorkFlowDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaWorkFlow List appears if save successful"
                    text=".*${oaWorkFlowList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlow.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaWorkFlow -->
    <target name="DeleteOaWorkFlow"
        description="Deletes existing OaWorkFlow">
        <webtest name="deleteOaWorkFlow">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlow link" url="/editOaWorkFlow.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaWorkFlow List" text=".*${oaWorkFlowList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlow.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaWorkFlows"
        description="Tests search for and displaying all oaWorkFlows">
        <webtest name="searchOaWorkFlows">
            &config;
            <steps>
                &login;
                <invoke description="click View OaWorkFlows link" url="/oaWorkFlows.html"/>
                <verifytitle description="we should see the oaWorkFlowList title"
                    text=".*${oaWorkFlowList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaWorkFlow-END-->

<!--OaWorkFlowCheck-START-->
    <!-- runs oaWorkFlowCheck-related tests -->
    <target name="OaWorkFlowCheckTests"
        depends="SearchOaWorkFlowChecks,EditOaWorkFlowCheck,SaveOaWorkFlowCheck,AddOaWorkFlowCheck,DeleteOaWorkFlowCheck"
        description="Call and executes all oaWorkFlowCheck test cases (targets)">
        <echo>Successfully ran all OaWorkFlowCheck JSP tests!</echo>
    </target>

    <!-- Verify the edit oaWorkFlowCheck screen displays without errors -->
    <target name="EditOaWorkFlowCheck"
        description="Tests editing an existing OaWorkFlowCheck's information">
        <webtest name="editOaWorkFlowCheck">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheck link" url="/editOaWorkFlowCheck.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowCheckDetail title"
                    text=".*${oaWorkFlowCheckDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaWorkFlowCheck and then save -->
    <target name="SaveOaWorkFlowCheck"
        description="Tests editing and saving a oaWorkFlowCheck">
        <webtest name="saveOaWorkFlowCheck">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheck link" url="/editOaWorkFlowCheck.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowCheckDetail title"
                    text=".*${oaWorkFlowCheckDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaWorkFlowCheckDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaWorkFlowCheck -->
    <target name="AddOaWorkFlowCheck"
        description="Adds a new OaWorkFlowCheck">
        <webtest name="addOaWorkFlowCheck">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaWorkFlowCheck.html"/>
                <verifytitle description="we should see the oaWorkFlowCheckDetail title"
                    text=".*${oaWorkFlowCheckDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaWorkFlowCheck List appears if save successful"
                    text=".*${oaWorkFlowCheckList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowCheck.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaWorkFlowCheck -->
    <target name="DeleteOaWorkFlowCheck"
        description="Deletes existing OaWorkFlowCheck">
        <webtest name="deleteOaWorkFlowCheck">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheck link" url="/editOaWorkFlowCheck.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaWorkFlowCheck List" text=".*${oaWorkFlowCheckList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowCheck.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaWorkFlowChecks"
        description="Tests search for and displaying all oaWorkFlowChecks">
        <webtest name="searchOaWorkFlowChecks">
            &config;
            <steps>
                &login;
                <invoke description="click View OaWorkFlowChecks link" url="/oaWorkFlowChecks.html"/>
                <verifytitle description="we should see the oaWorkFlowCheckList title"
                    text=".*${oaWorkFlowCheckList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaWorkFlowCheck-END-->

<!--OaWorkFlowCheckResult-START-->
    <!-- runs oaWorkFlowCheckResult-related tests -->
    <target name="OaWorkFlowCheckResultTests"
        depends="SearchOaWorkFlowCheckResults,EditOaWorkFlowCheckResult,SaveOaWorkFlowCheckResult,AddOaWorkFlowCheckResult,DeleteOaWorkFlowCheckResult"
        description="Call and executes all oaWorkFlowCheckResult test cases (targets)">
        <echo>Successfully ran all OaWorkFlowCheckResult JSP tests!</echo>
    </target>

    <!-- Verify the edit oaWorkFlowCheckResult screen displays without errors -->
    <target name="EditOaWorkFlowCheckResult"
        description="Tests editing an existing OaWorkFlowCheckResult's information">
        <webtest name="editOaWorkFlowCheckResult">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheckResult link" url="/editOaWorkFlowCheckResult.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowCheckResultDetail title"
                    text=".*${oaWorkFlowCheckResultDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaWorkFlowCheckResult and then save -->
    <target name="SaveOaWorkFlowCheckResult"
        description="Tests editing and saving a oaWorkFlowCheckResult">
        <webtest name="saveOaWorkFlowCheckResult">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheckResult link" url="/editOaWorkFlowCheckResult.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowCheckResultDetail title"
                    text=".*${oaWorkFlowCheckResultDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaWorkFlowCheckResultDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaWorkFlowCheckResult -->
    <target name="AddOaWorkFlowCheckResult"
        description="Adds a new OaWorkFlowCheckResult">
        <webtest name="addOaWorkFlowCheckResult">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaWorkFlowCheckResult.html"/>
                <verifytitle description="we should see the oaWorkFlowCheckResultDetail title"
                    text=".*${oaWorkFlowCheckResultDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaWorkFlowCheckResult List appears if save successful"
                    text=".*${oaWorkFlowCheckResultList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowCheckResult.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaWorkFlowCheckResult -->
    <target name="DeleteOaWorkFlowCheckResult"
        description="Deletes existing OaWorkFlowCheckResult">
        <webtest name="deleteOaWorkFlowCheckResult">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheckResult link" url="/editOaWorkFlowCheckResult.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaWorkFlowCheckResult List" text=".*${oaWorkFlowCheckResultList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowCheckResult.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaWorkFlowCheckResults"
        description="Tests search for and displaying all oaWorkFlowCheckResults">
        <webtest name="searchOaWorkFlowCheckResults">
            &config;
            <steps>
                &login;
                <invoke description="click View OaWorkFlowCheckResults link" url="/oaWorkFlowCheckResults.html"/>
                <verifytitle description="we should see the oaWorkFlowCheckResultList title"
                    text=".*${oaWorkFlowCheckResultList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaWorkFlowCheckResult-END-->

<!--OaWorkFlowType-START-->
    <!-- runs oaWorkFlowType-related tests -->
    <target name="OaWorkFlowTypeTests"
        depends="SearchOaWorkFlowTypes,EditOaWorkFlowType,SaveOaWorkFlowType,AddOaWorkFlowType,DeleteOaWorkFlowType"
        description="Call and executes all oaWorkFlowType test cases (targets)">
        <echo>Successfully ran all OaWorkFlowType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaWorkFlowType screen displays without errors -->
    <target name="EditOaWorkFlowType"
        description="Tests editing an existing OaWorkFlowType's information">
        <webtest name="editOaWorkFlowType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowType link" url="/editOaWorkFlowType.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowTypeDetail title"
                    text=".*${oaWorkFlowTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaWorkFlowType and then save -->
    <target name="SaveOaWorkFlowType"
        description="Tests editing and saving a oaWorkFlowType">
        <webtest name="saveOaWorkFlowType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowType link" url="/editOaWorkFlowType.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowTypeDetail title"
                    text=".*${oaWorkFlowTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaWorkFlowTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaWorkFlowType -->
    <target name="AddOaWorkFlowType"
        description="Adds a new OaWorkFlowType">
        <webtest name="addOaWorkFlowType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaWorkFlowType.html"/>
                <verifytitle description="we should see the oaWorkFlowTypeDetail title"
                    text=".*${oaWorkFlowTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaWorkFlowType List appears if save successful"
                    text=".*${oaWorkFlowTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaWorkFlowType -->
    <target name="DeleteOaWorkFlowType"
        description="Deletes existing OaWorkFlowType">
        <webtest name="deleteOaWorkFlowType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowType link" url="/editOaWorkFlowType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaWorkFlowType List" text=".*${oaWorkFlowTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaWorkFlowTypes"
        description="Tests search for and displaying all oaWorkFlowTypes">
        <webtest name="searchOaWorkFlowTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaWorkFlowTypes link" url="/oaWorkFlowTypes.html"/>
                <verifytitle description="we should see the oaWorkFlowTypeList title"
                    text=".*${oaWorkFlowTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaWorkFlowType-END-->

<!--SysEnentState-START-->
    <!-- runs sysEnentState-related tests -->
    <target name="SysEnentStateTests"
        depends="SearchSysEnentStates,EditSysEnentState,SaveSysEnentState,AddSysEnentState,DeleteSysEnentState"
        description="Call and executes all sysEnentState test cases (targets)">
        <echo>Successfully ran all SysEnentState JSP tests!</echo>
    </target>

    <!-- Verify the edit sysEnentState screen displays without errors -->
    <target name="EditSysEnentState"
        description="Tests editing an existing SysEnentState's information">
        <webtest name="editSysEnentState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEnentState link" url="/editSysEnentState.html?id=1"/>
                <verifytitle description="we should see the sysEnentStateDetail title"
                    text=".*${sysEnentStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysEnentState and then save -->
    <target name="SaveSysEnentState"
        description="Tests editing and saving a sysEnentState">
        <webtest name="saveSysEnentState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEnentState link" url="/editSysEnentState.html?id=1"/>
                <verifytitle description="we should see the sysEnentStateDetail title"
                    text=".*${sysEnentStateDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysEnentStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysEnentState -->
    <target name="AddSysEnentState"
        description="Adds a new SysEnentState">
        <webtest name="addSysEnentState">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysEnentState.html"/>
                <verifytitle description="we should see the sysEnentStateDetail title"
                    text=".*${sysEnentStateDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysEnentState List appears if save successful"
                    text=".*${sysEnentStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEnentState.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysEnentState -->
    <target name="DeleteSysEnentState"
        description="Deletes existing SysEnentState">
        <webtest name="deleteSysEnentState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEnentState link" url="/editSysEnentState.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysEnentState List" text=".*${sysEnentStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEnentState.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysEnentStates"
        description="Tests search for and displaying all sysEnentStates">
        <webtest name="searchSysEnentStates">
            &config;
            <steps>
                &login;
                <invoke description="click View SysEnentStates link" url="/sysEnentStates.html"/>
                <verifytitle description="we should see the sysEnentStateList title"
                    text=".*${sysEnentStateList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysEnentState-END-->

<!--SysEnentType-START-->
    <!-- runs sysEnentType-related tests -->
    <target name="SysEnentTypeTests"
        depends="SearchSysEnentTypes,EditSysEnentType,SaveSysEnentType,AddSysEnentType,DeleteSysEnentType"
        description="Call and executes all sysEnentType test cases (targets)">
        <echo>Successfully ran all SysEnentType JSP tests!</echo>
    </target>

    <!-- Verify the edit sysEnentType screen displays without errors -->
    <target name="EditSysEnentType"
        description="Tests editing an existing SysEnentType's information">
        <webtest name="editSysEnentType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEnentType link" url="/editSysEnentType.html?id=1"/>
                <verifytitle description="we should see the sysEnentTypeDetail title"
                    text=".*${sysEnentTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysEnentType and then save -->
    <target name="SaveSysEnentType"
        description="Tests editing and saving a sysEnentType">
        <webtest name="saveSysEnentType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEnentType link" url="/editSysEnentType.html?id=1"/>
                <verifytitle description="we should see the sysEnentTypeDetail title"
                    text=".*${sysEnentTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysEnentTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysEnentType -->
    <target name="AddSysEnentType"
        description="Adds a new SysEnentType">
        <webtest name="addSysEnentType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysEnentType.html"/>
                <verifytitle description="we should see the sysEnentTypeDetail title"
                    text=".*${sysEnentTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysEnentType List appears if save successful"
                    text=".*${sysEnentTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEnentType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysEnentType -->
    <target name="DeleteSysEnentType"
        description="Deletes existing SysEnentType">
        <webtest name="deleteSysEnentType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEnentType link" url="/editSysEnentType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysEnentType List" text=".*${sysEnentTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEnentType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysEnentTypes"
        description="Tests search for and displaying all sysEnentTypes">
        <webtest name="searchSysEnentTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View SysEnentTypes link" url="/sysEnentTypes.html"/>
                <verifytitle description="we should see the sysEnentTypeList title"
                    text=".*${sysEnentTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysEnentType-END-->

<!--SysEvent-START-->
    <!-- runs sysEvent-related tests -->
    <target name="SysEventTests"
        depends="SearchSysEvents,EditSysEvent,SaveSysEvent,AddSysEvent,DeleteSysEvent"
        description="Call and executes all sysEvent test cases (targets)">
        <echo>Successfully ran all SysEvent JSP tests!</echo>
    </target>

    <!-- Verify the edit sysEvent screen displays without errors -->
    <target name="EditSysEvent"
        description="Tests editing an existing SysEvent's information">
        <webtest name="editSysEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEvent link" url="/editSysEvent.html?id=1"/>
                <verifytitle description="we should see the sysEventDetail title"
                    text=".*${sysEventDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysEvent and then save -->
    <target name="SaveSysEvent"
        description="Tests editing and saving a sysEvent">
        <webtest name="saveSysEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEvent link" url="/editSysEvent.html?id=1"/>
                <verifytitle description="we should see the sysEventDetail title"
                    text=".*${sysEventDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysEventDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysEvent -->
    <target name="AddSysEvent"
        description="Adds a new SysEvent">
        <webtest name="addSysEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysEvent.html"/>
                <verifytitle description="we should see the sysEventDetail title"
                    text=".*${sysEventDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysEvent List appears if save successful"
                    text=".*${sysEventList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEvent.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysEvent -->
    <target name="DeleteSysEvent"
        description="Deletes existing SysEvent">
        <webtest name="deleteSysEvent">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEvent link" url="/editSysEvent.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysEvent List" text=".*${sysEventList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEvent.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysEvents"
        description="Tests search for and displaying all sysEvents">
        <webtest name="searchSysEvents">
            &config;
            <steps>
                &login;
                <invoke description="click View SysEvents link" url="/sysEvents.html"/>
                <verifytitle description="we should see the sysEventList title"
                    text=".*${sysEventList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysEvent-END-->

<!--SysEventState-START-->
    <!-- runs sysEventState-related tests -->
    <target name="SysEventStateTests"
        depends="SearchSysEventStates,EditSysEventState,SaveSysEventState,AddSysEventState,DeleteSysEventState"
        description="Call and executes all sysEventState test cases (targets)">
        <echo>Successfully ran all SysEventState JSP tests!</echo>
    </target>

    <!-- Verify the edit sysEventState screen displays without errors -->
    <target name="EditSysEventState"
        description="Tests editing an existing SysEventState's information">
        <webtest name="editSysEventState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEventState link" url="/editSysEventState.html?id=1"/>
                <verifytitle description="we should see the sysEventStateDetail title"
                    text=".*${sysEventStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysEventState and then save -->
    <target name="SaveSysEventState"
        description="Tests editing and saving a sysEventState">
        <webtest name="saveSysEventState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEventState link" url="/editSysEventState.html?id=1"/>
                <verifytitle description="we should see the sysEventStateDetail title"
                    text=".*${sysEventStateDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysEventStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysEventState -->
    <target name="AddSysEventState"
        description="Adds a new SysEventState">
        <webtest name="addSysEventState">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysEventState.html"/>
                <verifytitle description="we should see the sysEventStateDetail title"
                    text=".*${sysEventStateDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysEventState List appears if save successful"
                    text=".*${sysEventStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEventState.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysEventState -->
    <target name="DeleteSysEventState"
        description="Deletes existing SysEventState">
        <webtest name="deleteSysEventState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEventState link" url="/editSysEventState.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysEventState List" text=".*${sysEventStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEventState.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysEventStates"
        description="Tests search for and displaying all sysEventStates">
        <webtest name="searchSysEventStates">
            &config;
            <steps>
                &login;
                <invoke description="click View SysEventStates link" url="/sysEventStates.html"/>
                <verifytitle description="we should see the sysEventStateList title"
                    text=".*${sysEventStateList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysEventState-END-->

<!--SysEventType-START-->
    <!-- runs sysEventType-related tests -->
    <target name="SysEventTypeTests"
        depends="SearchSysEventTypes,EditSysEventType,SaveSysEventType,AddSysEventType,DeleteSysEventType"
        description="Call and executes all sysEventType test cases (targets)">
        <echo>Successfully ran all SysEventType JSP tests!</echo>
    </target>

    <!-- Verify the edit sysEventType screen displays without errors -->
    <target name="EditSysEventType"
        description="Tests editing an existing SysEventType's information">
        <webtest name="editSysEventType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEventType link" url="/editSysEventType.html?id=1"/>
                <verifytitle description="we should see the sysEventTypeDetail title"
                    text=".*${sysEventTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysEventType and then save -->
    <target name="SaveSysEventType"
        description="Tests editing and saving a sysEventType">
        <webtest name="saveSysEventType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEventType link" url="/editSysEventType.html?id=1"/>
                <verifytitle description="we should see the sysEventTypeDetail title"
                    text=".*${sysEventTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysEventTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysEventType -->
    <target name="AddSysEventType"
        description="Adds a new SysEventType">
        <webtest name="addSysEventType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysEventType.html"/>
                <verifytitle description="we should see the sysEventTypeDetail title"
                    text=".*${sysEventTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysEventType List appears if save successful"
                    text=".*${sysEventTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEventType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysEventType -->
    <target name="DeleteSysEventType"
        description="Deletes existing SysEventType">
        <webtest name="deleteSysEventType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysEventType link" url="/editSysEventType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysEventType List" text=".*${sysEventTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysEventType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysEventTypes"
        description="Tests search for and displaying all sysEventTypes">
        <webtest name="searchSysEventTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View SysEventTypes link" url="/sysEventTypes.html"/>
                <verifytitle description="we should see the sysEventTypeList title"
                    text=".*${sysEventTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysEventType-END-->

<!--SysPromptMode-START-->
    <!-- runs sysPromptMode-related tests -->
    <target name="SysPromptModeTests"
        depends="SearchSysPromptModes,EditSysPromptMode,SaveSysPromptMode,AddSysPromptMode,DeleteSysPromptMode"
        description="Call and executes all sysPromptMode test cases (targets)">
        <echo>Successfully ran all SysPromptMode JSP tests!</echo>
    </target>

    <!-- Verify the edit sysPromptMode screen displays without errors -->
    <target name="EditSysPromptMode"
        description="Tests editing an existing SysPromptMode's information">
        <webtest name="editSysPromptMode">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysPromptMode link" url="/editSysPromptMode.html?id=1"/>
                <verifytitle description="we should see the sysPromptModeDetail title"
                    text=".*${sysPromptModeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysPromptMode and then save -->
    <target name="SaveSysPromptMode"
        description="Tests editing and saving a sysPromptMode">
        <webtest name="saveSysPromptMode">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysPromptMode link" url="/editSysPromptMode.html?id=1"/>
                <verifytitle description="we should see the sysPromptModeDetail title"
                    text=".*${sysPromptModeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysPromptModeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysPromptMode -->
    <target name="AddSysPromptMode"
        description="Adds a new SysPromptMode">
        <webtest name="addSysPromptMode">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysPromptMode.html"/>
                <verifytitle description="we should see the sysPromptModeDetail title"
                    text=".*${sysPromptModeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysPromptMode List appears if save successful"
                    text=".*${sysPromptModeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysPromptMode.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysPromptMode -->
    <target name="DeleteSysPromptMode"
        description="Deletes existing SysPromptMode">
        <webtest name="deleteSysPromptMode">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysPromptMode link" url="/editSysPromptMode.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysPromptMode List" text=".*${sysPromptModeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysPromptMode.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysPromptModes"
        description="Tests search for and displaying all sysPromptModes">
        <webtest name="searchSysPromptModes">
            &config;
            <steps>
                &login;
                <invoke description="click View SysPromptModes link" url="/sysPromptModes.html"/>
                <verifytitle description="we should see the sysPromptModeList title"
                    text=".*${sysPromptModeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysPromptMode-END-->

<!--SysUserType-START-->
    <!-- runs sysUserType-related tests -->
    <target name="SysUserTypeTests"
        depends="SearchSysUserTypes,EditSysUserType,SaveSysUserType,AddSysUserType,DeleteSysUserType"
        description="Call and executes all sysUserType test cases (targets)">
        <echo>Successfully ran all SysUserType JSP tests!</echo>
    </target>

    <!-- Verify the edit sysUserType screen displays without errors -->
    <target name="EditSysUserType"
        description="Tests editing an existing SysUserType's information">
        <webtest name="editSysUserType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysUserType link" url="/editSysUserType.html?id=1"/>
                <verifytitle description="we should see the sysUserTypeDetail title"
                    text=".*${sysUserTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysUserType and then save -->
    <target name="SaveSysUserType"
        description="Tests editing and saving a sysUserType">
        <webtest name="saveSysUserType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysUserType link" url="/editSysUserType.html?id=1"/>
                <verifytitle description="we should see the sysUserTypeDetail title"
                    text=".*${sysUserTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysUserTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysUserType -->
    <target name="AddSysUserType"
        description="Adds a new SysUserType">
        <webtest name="addSysUserType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysUserType.html"/>
                <verifytitle description="we should see the sysUserTypeDetail title"
                    text=".*${sysUserTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysUserType List appears if save successful"
                    text=".*${sysUserTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysUserType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysUserType -->
    <target name="DeleteSysUserType"
        description="Deletes existing SysUserType">
        <webtest name="deleteSysUserType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysUserType link" url="/editSysUserType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysUserType List" text=".*${sysUserTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysUserType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysUserTypes"
        description="Tests search for and displaying all sysUserTypes">
        <webtest name="searchSysUserTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View SysUserTypes link" url="/sysUserTypes.html"/>
                <verifytitle description="we should see the sysUserTypeList title"
                    text=".*${sysUserTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysUserType-END-->

<!--OaWorkFlowCheckState-START-->
    <!-- runs oaWorkFlowCheckState-related tests -->
    <target name="OaWorkFlowCheckStateTests"
        depends="SearchOaWorkFlowCheckStates,EditOaWorkFlowCheckState,SaveOaWorkFlowCheckState,AddOaWorkFlowCheckState,DeleteOaWorkFlowCheckState"
        description="Call and executes all oaWorkFlowCheckState test cases (targets)">
        <echo>Successfully ran all OaWorkFlowCheckState JSP tests!</echo>
    </target>

    <!-- Verify the edit oaWorkFlowCheckState screen displays without errors -->
    <target name="EditOaWorkFlowCheckState"
        description="Tests editing an existing OaWorkFlowCheckState's information">
        <webtest name="editOaWorkFlowCheckState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheckState link" url="/editOaWorkFlowCheckState.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowCheckStateDetail title"
                    text=".*${oaWorkFlowCheckStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaWorkFlowCheckState and then save -->
    <target name="SaveOaWorkFlowCheckState"
        description="Tests editing and saving a oaWorkFlowCheckState">
        <webtest name="saveOaWorkFlowCheckState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheckState link" url="/editOaWorkFlowCheckState.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowCheckStateDetail title"
                    text=".*${oaWorkFlowCheckStateDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaWorkFlowCheckStateDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaWorkFlowCheckState -->
    <target name="AddOaWorkFlowCheckState"
        description="Adds a new OaWorkFlowCheckState">
        <webtest name="addOaWorkFlowCheckState">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaWorkFlowCheckState.html"/>
                <verifytitle description="we should see the oaWorkFlowCheckStateDetail title"
                    text=".*${oaWorkFlowCheckStateDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaWorkFlowCheckState List appears if save successful"
                    text=".*${oaWorkFlowCheckStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowCheckState.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaWorkFlowCheckState -->
    <target name="DeleteOaWorkFlowCheckState"
        description="Deletes existing OaWorkFlowCheckState">
        <webtest name="deleteOaWorkFlowCheckState">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowCheckState link" url="/editOaWorkFlowCheckState.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaWorkFlowCheckState List" text=".*${oaWorkFlowCheckStateList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowCheckState.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaWorkFlowCheckStates"
        description="Tests search for and displaying all oaWorkFlowCheckStates">
        <webtest name="searchOaWorkFlowCheckStates">
            &config;
            <steps>
                &login;
                <invoke description="click View OaWorkFlowCheckStates link" url="/oaWorkFlowCheckStates.html"/>
                <verifytitle description="we should see the oaWorkFlowCheckStateList title"
                    text=".*${oaWorkFlowCheckStateList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaWorkFlowCheckState-END-->

<!--OaWorkFlowMoveType-START-->
    <!-- runs oaWorkFlowMoveType-related tests -->
    <target name="OaWorkFlowMoveTypeTests"
        depends="SearchOaWorkFlowMoveTypes,EditOaWorkFlowMoveType,SaveOaWorkFlowMoveType,AddOaWorkFlowMoveType,DeleteOaWorkFlowMoveType"
        description="Call and executes all oaWorkFlowMoveType test cases (targets)">
        <echo>Successfully ran all OaWorkFlowMoveType JSP tests!</echo>
    </target>

    <!-- Verify the edit oaWorkFlowMoveType screen displays without errors -->
    <target name="EditOaWorkFlowMoveType"
        description="Tests editing an existing OaWorkFlowMoveType's information">
        <webtest name="editOaWorkFlowMoveType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowMoveType link" url="/editOaWorkFlowMoveType.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowMoveTypeDetail title"
                    text=".*${oaWorkFlowMoveTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaWorkFlowMoveType and then save -->
    <target name="SaveOaWorkFlowMoveType"
        description="Tests editing and saving a oaWorkFlowMoveType">
        <webtest name="saveOaWorkFlowMoveType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowMoveType link" url="/editOaWorkFlowMoveType.html?id=1"/>
                <verifytitle description="we should see the oaWorkFlowMoveTypeDetail title"
                    text=".*${oaWorkFlowMoveTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaWorkFlowMoveTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaWorkFlowMoveType -->
    <target name="AddOaWorkFlowMoveType"
        description="Adds a new OaWorkFlowMoveType">
        <webtest name="addOaWorkFlowMoveType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaWorkFlowMoveType.html"/>
                <verifytitle description="we should see the oaWorkFlowMoveTypeDetail title"
                    text=".*${oaWorkFlowMoveTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaWorkFlowMoveType List appears if save successful"
                    text=".*${oaWorkFlowMoveTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowMoveType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaWorkFlowMoveType -->
    <target name="DeleteOaWorkFlowMoveType"
        description="Deletes existing OaWorkFlowMoveType">
        <webtest name="deleteOaWorkFlowMoveType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaWorkFlowMoveType link" url="/editOaWorkFlowMoveType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaWorkFlowMoveType List" text=".*${oaWorkFlowMoveTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaWorkFlowMoveType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaWorkFlowMoveTypes"
        description="Tests search for and displaying all oaWorkFlowMoveTypes">
        <webtest name="searchOaWorkFlowMoveTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View OaWorkFlowMoveTypes link" url="/oaWorkFlowMoveTypes.html"/>
                <verifytitle description="we should see the oaWorkFlowMoveTypeList title"
                    text=".*${oaWorkFlowMoveTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaWorkFlowMoveType-END-->

<!--SysResource-START-->
    <!-- runs sysResource-related tests -->
    <target name="SysResourceTests"
        depends="SearchSysResources,EditSysResource,SaveSysResource,AddSysResource,DeleteSysResource"
        description="Call and executes all sysResource test cases (targets)">
        <echo>Successfully ran all SysResource JSP tests!</echo>
    </target>

    <!-- Verify the edit sysResource screen displays without errors -->
    <target name="EditSysResource"
        description="Tests editing an existing SysResource's information">
        <webtest name="editSysResource">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysResource link" url="/editSysResource.html?id=1"/>
                <verifytitle description="we should see the sysResourceDetail title"
                    text=".*${sysResourceDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysResource and then save -->
    <target name="SaveSysResource"
        description="Tests editing and saving a sysResource">
        <webtest name="saveSysResource">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysResource link" url="/editSysResource.html?id=1"/>
                <verifytitle description="we should see the sysResourceDetail title"
                    text=".*${sysResourceDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysResourceDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysResource -->
    <target name="AddSysResource"
        description="Adds a new SysResource">
        <webtest name="addSysResource">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysResource.html"/>
                <verifytitle description="we should see the sysResourceDetail title"
                    text=".*${sysResourceDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysResource List appears if save successful"
                    text=".*${sysResourceList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysResource.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysResource -->
    <target name="DeleteSysResource"
        description="Deletes existing SysResource">
        <webtest name="deleteSysResource">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysResource link" url="/editSysResource.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysResource List" text=".*${sysResourceList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysResource.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysResources"
        description="Tests search for and displaying all sysResources">
        <webtest name="searchSysResources">
            &config;
            <steps>
                &login;
                <invoke description="click View SysResources link" url="/sysResources.html"/>
                <verifytitle description="we should see the sysResourceList title"
                    text=".*${sysResourceList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysResource-END-->

<!--ResourceSort-START-->
    <!-- runs resourceSort-related tests -->
    <target name="ResourceSortTests"
        depends="SearchResourceSorts,EditResourceSort,SaveResourceSort,AddResourceSort,DeleteResourceSort"
        description="Call and executes all resourceSort test cases (targets)">
        <echo>Successfully ran all ResourceSort JSP tests!</echo>
    </target>

    <!-- Verify the edit resourceSort screen displays without errors -->
    <target name="EditResourceSort"
        description="Tests editing an existing ResourceSort's information">
        <webtest name="editResourceSort">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ResourceSort link" url="/editResourceSort.html?id=1"/>
                <verifytitle description="we should see the resourceSortDetail title"
                    text=".*${resourceSortDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a resourceSort and then save -->
    <target name="SaveResourceSort"
        description="Tests editing and saving a resourceSort">
        <webtest name="saveResourceSort">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ResourceSort link" url="/editResourceSort.html?id=1"/>
                <verifytitle description="we should see the resourceSortDetail title"
                    text=".*${resourceSortDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${resourceSortDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new ResourceSort -->
    <target name="AddResourceSort"
        description="Adds a new ResourceSort">
        <webtest name="addResourceSort">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editResourceSort.html"/>
                <verifytitle description="we should see the resourceSortDetail title"
                    text=".*${resourceSortDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="ResourceSort List appears if save successful"
                    text=".*${resourceSortList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${resourceSort.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing resourceSort -->
    <target name="DeleteResourceSort"
        description="Deletes existing ResourceSort">
        <webtest name="deleteResourceSort">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ResourceSort link" url="/editResourceSort.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display ResourceSort List" text=".*${resourceSortList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${resourceSort.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchResourceSorts"
        description="Tests search for and displaying all resourceSorts">
        <webtest name="searchResourceSorts">
            &config;
            <steps>
                &login;
                <invoke description="click View ResourceSorts link" url="/resourceSorts.html"/>
                <verifytitle description="we should see the resourceSortList title"
                    text=".*${resourceSortList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--ResourceSort-END-->

<!--ContractTargetMonth-START-->
    <!-- runs contractTargetMonth-related tests -->
    <target name="ContractTargetMonthTests"
        depends="SearchContractTargetMonths,EditContractTargetMonth,SaveContractTargetMonth,AddContractTargetMonth,DeleteContractTargetMonth"
        description="Call and executes all contractTargetMonth test cases (targets)">
        <echo>Successfully ran all ContractTargetMonth JSP tests!</echo>
    </target>

    <!-- Verify the edit contractTargetMonth screen displays without errors -->
    <target name="EditContractTargetMonth"
        description="Tests editing an existing ContractTargetMonth's information">
        <webtest name="editContractTargetMonth">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractTargetMonth link" url="/editContractTargetMonth.html?id=1"/>
                <verifytitle description="we should see the contractTargetMonthDetail title"
                    text=".*${contractTargetMonthDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a contractTargetMonth and then save -->
    <target name="SaveContractTargetMonth"
        description="Tests editing and saving a contractTargetMonth">
        <webtest name="saveContractTargetMonth">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractTargetMonth link" url="/editContractTargetMonth.html?id=1"/>
                <verifytitle description="we should see the contractTargetMonthDetail title"
                    text=".*${contractTargetMonthDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${contractTargetMonthDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new ContractTargetMonth -->
    <target name="AddContractTargetMonth"
        description="Adds a new ContractTargetMonth">
        <webtest name="addContractTargetMonth">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editContractTargetMonth.html"/>
                <verifytitle description="we should see the contractTargetMonthDetail title"
                    text=".*${contractTargetMonthDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set monthDate" name="monthDate" value="2061207443"/>
                <setinputfield description="set monthTarg" name="monthTarg" value="8.529390645074815E307"/>
                <setinputfield description="set monthReal" name="monthReal" value="1.2372434942334825E308"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="ContractTargetMonth List appears if save successful"
                    text=".*${contractTargetMonthList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contractTargetMonth.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing contractTargetMonth -->
    <target name="DeleteContractTargetMonth"
        description="Deletes existing ContractTargetMonth">
        <webtest name="deleteContractTargetMonth">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ContractTargetMonth link" url="/editContractTargetMonth.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display ContractTargetMonth List" text=".*${contractTargetMonthList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${contractTargetMonth.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchContractTargetMonths"
        description="Tests search for and displaying all contractTargetMonths">
        <webtest name="searchContractTargetMonths">
            &config;
            <steps>
                &login;
                <invoke description="click View ContractTargetMonths link" url="/contractTargetMonths.html"/>
                <verifytitle description="we should see the contractTargetMonthList title"
                    text=".*${contractTargetMonthList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--ContractTargetMonth-END-->

<!--OaTeleExpenses-START-->
    <!-- runs oaTeleExpenses-related tests -->
    <target name="OaTeleExpensesTests"
        depends="SearchOaTeleExpensess,EditOaTeleExpenses,SaveOaTeleExpenses,AddOaTeleExpenses,DeleteOaTeleExpenses"
        description="Call and executes all oaTeleExpenses test cases (targets)">
        <echo>Successfully ran all OaTeleExpenses JSP tests!</echo>
    </target>

    <!-- Verify the edit oaTeleExpenses screen displays without errors -->
    <target name="EditOaTeleExpenses"
        description="Tests editing an existing OaTeleExpenses's information">
        <webtest name="editOaTeleExpenses">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaTeleExpenses link" url="/editOaTeleExpenses.html?id=1"/>
                <verifytitle description="we should see the oaTeleExpensesDetail title"
                    text=".*${oaTeleExpensesDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a oaTeleExpenses and then save -->
    <target name="SaveOaTeleExpenses"
        description="Tests editing and saving a oaTeleExpenses">
        <webtest name="saveOaTeleExpenses">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaTeleExpenses link" url="/editOaTeleExpenses.html?id=1"/>
                <verifytitle description="we should see the oaTeleExpensesDetail title"
                    text=".*${oaTeleExpensesDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${oaTeleExpensesDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OaTeleExpenses -->
    <target name="AddOaTeleExpenses"
        description="Adds a new OaTeleExpenses">
        <webtest name="addOaTeleExpenses">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editOaTeleExpenses.html"/>
                <verifytitle description="we should see the oaTeleExpensesDetail title"
                    text=".*${oaTeleExpensesDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set expenses" name="expenses" value="3.599267239830973E307"/>
                <setinputfield description="set registerDate" name="registerDate" value="1287032592"/>
                <setinputfield description="set branchId" name="branchId" value="2934809544378962944"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="OaTeleExpenses List appears if save successful"
                    text=".*${oaTeleExpensesList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaTeleExpenses.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing oaTeleExpenses -->
    <target name="DeleteOaTeleExpenses"
        description="Deletes existing OaTeleExpenses">
        <webtest name="deleteOaTeleExpenses">
            &config;
            <steps>
                &login;
                <invoke description="click Edit OaTeleExpenses link" url="/editOaTeleExpenses.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OaTeleExpenses List" text=".*${oaTeleExpensesList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${oaTeleExpenses.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchOaTeleExpensess"
        description="Tests search for and displaying all oaTeleExpensess">
        <webtest name="searchOaTeleExpensess">
            &config;
            <steps>
                &login;
                <invoke description="click View OaTeleExpensess link" url="/oaTeleExpensess.html"/>
                <verifytitle description="we should see the oaTeleExpensesList title"
                    text=".*${oaTeleExpensesList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--OaTeleExpenses-END-->

<!--SysMenu-START-->
    <!-- runs sysMenu-related tests -->
    <target name="SysMenuTests"
        depends="SearchSysMenus,EditSysMenu,SaveSysMenu,AddSysMenu,DeleteSysMenu"
        description="Call and executes all sysMenu test cases (targets)">
        <echo>Successfully ran all SysMenu JSP tests!</echo>
    </target>

    <!-- Verify the edit sysMenu screen displays without errors -->
    <target name="EditSysMenu"
        description="Tests editing an existing SysMenu's information">
        <webtest name="editSysMenu">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysMenu link" url="/editSysMenu.html?id=1"/>
                <verifytitle description="we should see the sysMenuDetail title"
                    text=".*${sysMenuDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysMenu and then save -->
    <target name="SaveSysMenu"
        description="Tests editing and saving a sysMenu">
        <webtest name="saveSysMenu">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysMenu link" url="/editSysMenu.html?id=1"/>
                <verifytitle description="we should see the sysMenuDetail title"
                    text=".*${sysMenuDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set name" name="name" value="name"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysMenuDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysMenu -->
    <target name="AddSysMenu"
        description="Adds a new SysMenu">
        <webtest name="addSysMenu">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysMenu.html"/>
                <verifytitle description="we should see the sysMenuDetail title"
                    text=".*${sysMenuDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set name" name="name" value="RdAlHmRjDhLvNpPaMrKkUfZiVzFxClGv"/>
                <setinputfield description="set displayNo" name="displayNo" value="841328929"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysMenu List appears if save successful"
                    text=".*${sysMenuList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysMenu.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysMenu -->
    <target name="DeleteSysMenu"
        description="Deletes existing SysMenu">
        <webtest name="deleteSysMenu">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysMenu link" url="/editSysMenu.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysMenu List" text=".*${sysMenuList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysMenu.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysMenus"
        description="Tests search for and displaying all sysMenus">
        <webtest name="searchSysMenus">
            &config;
            <steps>
                &login;
                <invoke description="click View SysMenus link" url="/sysMenus.html"/>
                <verifytitle description="we should see the sysMenuList title"
                    text=".*${sysMenuList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysMenu-END-->

<!--SysParam-START-->
    <!-- runs sysParam-related tests -->
    <target name="SysParamTests"
        depends="SearchSysParams,EditSysParam,SaveSysParam,AddSysParam,DeleteSysParam"
        description="Call and executes all sysParam test cases (targets)">
        <echo>Successfully ran all SysParam JSP tests!</echo>
    </target>

    <!-- Verify the edit sysParam screen displays without errors -->
    <target name="EditSysParam"
        description="Tests editing an existing SysParam's information">
        <webtest name="editSysParam">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysParam link" url="/editSysParam.html?id=1"/>
                <verifytitle description="we should see the sysParamDetail title"
                    text=".*${sysParamDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysParam and then save -->
    <target name="SaveSysParam"
        description="Tests editing and saving a sysParam">
        <webtest name="saveSysParam">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysParam link" url="/editSysParam.html?id=1"/>
                <verifytitle description="we should see the sysParamDetail title"
                    text=".*${sysParamDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set name" name="name" value="name"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysParamDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysParam -->
    <target name="AddSysParam"
        description="Adds a new SysParam">
        <webtest name="addSysParam">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysParam.html"/>
                <verifytitle description="we should see the sysParamDetail title"
                    text=".*${sysParamDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set name" name="name" value="MzOfRxSeMvQyFmJwFsAwXoLoPdWhDdSnHuUeIiNsBxEhDmKyFhAnUbKuGxIkIoEzKpTjPmTiCoKyBhBtBvAnMoJbMmQxZsEiXwSlDoHxTjIaWfZzEeSyDqZoUrNiHvYm"/>
                <setinputfield description="set value" name="value" value="354845632"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysParam List appears if save successful"
                    text=".*${sysParamList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysParam.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysParam -->
    <target name="DeleteSysParam"
        description="Deletes existing SysParam">
        <webtest name="deleteSysParam">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysParam link" url="/editSysParam.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysParam List" text=".*${sysParamList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysParam.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysParams"
        description="Tests search for and displaying all sysParams">
        <webtest name="searchSysParams">
            &config;
            <steps>
                &login;
                <invoke description="click View SysParams link" url="/sysParams.html"/>
                <verifytitle description="we should see the sysParamList title"
                    text=".*${sysParamList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysParam-END-->

<!--MatterType-START-->
    <!-- runs matterType-related tests -->
    <target name="MatterTypeTests"
        depends="SearchMatterTypes,EditMatterType,SaveMatterType,AddMatterType,DeleteMatterType"
        description="Call and executes all matterType test cases (targets)">
        <echo>Successfully ran all MatterType JSP tests!</echo>
    </target>

    <!-- Verify the edit matterType screen displays without errors -->
    <target name="EditMatterType"
        description="Tests editing an existing MatterType's information">
        <webtest name="editMatterType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit MatterType link" url="/editMatterType.html?id=1"/>
                <verifytitle description="we should see the matterTypeDetail title"
                    text=".*${matterTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a matterType and then save -->
    <target name="SaveMatterType"
        description="Tests editing and saving a matterType">
        <webtest name="saveMatterType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit MatterType link" url="/editMatterType.html?id=1"/>
                <verifytitle description="we should see the matterTypeDetail title"
                    text=".*${matterTypeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${matterTypeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new MatterType -->
    <target name="AddMatterType"
        description="Adds a new MatterType">
        <webtest name="addMatterType">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editMatterType.html"/>
                <verifytitle description="we should see the matterTypeDetail title"
                    text=".*${matterTypeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="MatterType List appears if save successful"
                    text=".*${matterTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${matterType.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing matterType -->
    <target name="DeleteMatterType"
        description="Deletes existing MatterType">
        <webtest name="deleteMatterType">
            &config;
            <steps>
                &login;
                <invoke description="click Edit MatterType link" url="/editMatterType.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display MatterType List" text=".*${matterTypeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${matterType.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchMatterTypes"
        description="Tests search for and displaying all matterTypes">
        <webtest name="searchMatterTypes">
            &config;
            <steps>
                &login;
                <invoke description="click View MatterTypes link" url="/matterTypes.html"/>
                <verifytitle description="we should see the matterTypeList title"
                    text=".*${matterTypeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--MatterType-END-->

<!--SysSequence-START-->
    <!-- runs sysSequence-related tests -->
    <target name="SysSequenceTests"
        depends="SearchSysSequences,EditSysSequence,SaveSysSequence,AddSysSequence,DeleteSysSequence"
        description="Call and executes all sysSequence test cases (targets)">
        <echo>Successfully ran all SysSequence JSP tests!</echo>
    </target>

    <!-- Verify the edit sysSequence screen displays without errors -->
    <target name="EditSysSequence"
        description="Tests editing an existing SysSequence's information">
        <webtest name="editSysSequence">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysSequence link" url="/editSysSequence.html?sequenceID=1"/>
                <verifytitle description="we should see the sysSequenceDetail title"
                    text=".*${sysSequenceDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a sysSequence and then save -->
    <target name="SaveSysSequence"
        description="Tests editing and saving a sysSequence">
        <webtest name="saveSysSequence">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysSequence link" url="/editSysSequence.html?sequenceID=1"/>
                <verifytitle description="we should see the sysSequenceDetail title"
                    text=".*${sysSequenceDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${sysSequenceDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new SysSequence -->
    <target name="AddSysSequence"
        description="Adds a new SysSequence">
        <webtest name="addSysSequence">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSysSequence.html"/>
                <verifytitle description="we should see the sysSequenceDetail title"
                    text=".*${sysSequenceDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="SysSequence List appears if save successful"
                    text=".*${sysSequenceList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysSequence.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing sysSequence -->
    <target name="DeleteSysSequence"
        description="Deletes existing SysSequence">
        <webtest name="deleteSysSequence">
            &config;
            <steps>
                &login;
                <invoke description="click Edit SysSequence link" url="/editSysSequence.html?sequenceID=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display SysSequence List" text=".*${sysSequenceList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${sysSequence.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSysSequences"
        description="Tests search for and displaying all sysSequences">
        <webtest name="searchSysSequences">
            &config;
            <steps>
                &login;
                <invoke description="click View SysSequences link" url="/sysSequences.html"/>
                <verifytitle description="we should see the sysSequenceList title"
                    text=".*${sysSequenceList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--SysSequence-END-->

<!--PublishArrange-START-->
    <!-- runs publishArrange-related tests -->
    <target name="PublishArrangeTests"
        depends="SearchPublishArranges,EditPublishArrange,SavePublishArrange,AddPublishArrange,DeletePublishArrange"
        description="Call and executes all publishArrange test cases (targets)">
        <echo>Successfully ran all PublishArrange JSP tests!</echo>
    </target>

    <!-- Verify the edit publishArrange screen displays without errors -->
    <target name="EditPublishArrange"
        description="Tests editing an existing PublishArrange's information">
        <webtest name="editPublishArrange">
            &config;
            <steps>
                &login;
                <invoke description="click Edit PublishArrange link" url="/editPublishArrange.html?id=1"/>
                <verifytitle description="we should see the publishArrangeDetail title"
                    text=".*${publishArrangeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a publishArrange and then save -->
    <target name="SavePublishArrange"
        description="Tests editing and saving a publishArrange">
        <webtest name="savePublishArrange">
            &config;
            <steps>
                &login;
                <invoke description="click Edit PublishArrange link" url="/editPublishArrange.html?id=1"/>
                <verifytitle description="we should see the publishArrangeDetail title"
                    text=".*${publishArrangeDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set carrierName" name="carrierName" value="carrierName"/>
                <setinputfield description="set filePath" name="filePath" value="filePath"/>
                <setinputfield description="set resourceMeno" name="resourceMeno" value="resourceMeno"/>
                <setinputfield description="set resourceName" name="resourceName" value="resourceName"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${publishArrangeDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new PublishArrange -->
    <target name="AddPublishArrange"
        description="Adds a new PublishArrange">
        <webtest name="addPublishArrange">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editPublishArrange.html"/>
                <verifytitle description="we should see the publishArrangeDetail title"
                    text=".*${publishArrangeDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set carrierId" name="carrierId" value="8584674142868460544"/>
                <setinputfield description="set carrierName" name="carrierName" value="QuLiCrHuCfXaJwYpSbRaVsVqUsGfMgSqHhCkWgEzCvKoHzIyBnIdZkUxZtKsRnTgNiHqGfBtSvUmVzWkHrOtHhHuSvGxEpOzRfFqGgNlDaMaHoRyEyQeKcCxOqPeVvEr"/>
                <setinputfield description="set filePath" name="filePath" value=""/>
                <setinputfield description="set isEnable" name="isEnable" value="N"/>
                <setinputfield description="set isLocked" name="isLocked" value="N"/>
                <setinputfield description="set publishDate" name="publishDate" value="1246072904"/>
                <setinputfield description="set resourceId" name="resourceId" value="7128308142708623360"/>
                <setinputfield description="set resourceMeno" name="resourceMeno" value="CkRvSiVrMjWsDxZwLrWqJgSbVxMrEhDcDuDpOnOkLjHaYmAbYnLyDlDaWxMvKbYbRcNyRlPoZsAnTdAoKaBuPrIgTyLwCaHzDuLuQsClOrLqIzIcNvIkKtUsXjVsEiYg"/>
                <setinputfield description="set resourceName" name="resourceName" value="XuZeRnMlLsGiEzYkPeMqPqWmZtBoWwFcEpMdRfEnFsCwFvWbLzGrVkNzOjVzRxNtZtTqScOmFkDwXqTpSoHjXhOySsHaGpVjUjIxRvBeWdBrLkJvNbPkTvTzJeUrDvNt"/>
                <setinputfield description="set resourceTotalTimes" name="resourceTotalTimes" value="2032925308"/>
                <setinputfield description="set resourceUsedTimes" name="resourceUsedTimes" value="159281053"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="PublishArrange List appears if save successful"
                    text=".*${publishArrangeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${publishArrange.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing publishArrange -->
    <target name="DeletePublishArrange"
        description="Deletes existing PublishArrange">
        <webtest name="deletePublishArrange">
            &config;
            <steps>
                &login;
                <invoke description="click Edit PublishArrange link" url="/editPublishArrange.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display PublishArrange List" text=".*${publishArrangeList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${publishArrange.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchPublishArranges"
        description="Tests search for and displaying all publishArranges">
        <webtest name="searchPublishArranges">
            &config;
            <steps>
                &login;
                <invoke description="click View PublishArranges link" url="/publishArranges.html"/>
                <verifytitle description="we should see the publishArrangeList title"
                    text=".*${publishArrangeList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--PublishArrange-END-->

<!--PublishArrangeDetail-START-->
    <!-- runs publishArrangeDetail-related tests -->
    <target name="PublishArrangeDetailTests"
        depends="SearchPublishArrangeDetails,EditPublishArrangeDetail,SavePublishArrangeDetail,AddPublishArrangeDetail,DeletePublishArrangeDetail"
        description="Call and executes all publishArrangeDetail test cases (targets)">
        <echo>Successfully ran all PublishArrangeDetail JSP tests!</echo>
    </target>

    <!-- Verify the edit publishArrangeDetail screen displays without errors -->
    <target name="EditPublishArrangeDetail"
        description="Tests editing an existing PublishArrangeDetail's information">
        <webtest name="editPublishArrangeDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Edit PublishArrangeDetail link" url="/editPublishArrangeDetail.html?id=1"/>
                <verifytitle description="we should see the publishArrangeDetailDetail title"
                    text=".*${publishArrangeDetailDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a publishArrangeDetail and then save -->
    <target name="SavePublishArrangeDetail"
        description="Tests editing and saving a publishArrangeDetail">
        <webtest name="savePublishArrangeDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Edit PublishArrangeDetail link" url="/editPublishArrangeDetail.html?id=1"/>
                <verifytitle description="we should see the publishArrangeDetailDetail title"
                    text=".*${publishArrangeDetailDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set specificValue" name="specificValue" value="specificValue"/>
                <setinputfield description="set customerName" name="customerName" value="customerName"/>
                <setinputfield description="set tapeCode" name="tapeCode" value="tapeCode"/>
                <setinputfield description="set matterEdit" name="matterEdit" value="matterEdit"/>
                <setinputfield description="set matterLength" name="matterLength" value="matterLength"/>
                <setinputfield description="set matterName" name="matterName" value="matterName"/>
                <setinputfield description="set specificName" name="specificName" value="specificName"/>
                <setinputfield description="set ownerUserName" name="ownerUserName" value="ownerUserName"/>
                <setinputfield description="set publishMemo" name="publishMemo" value="publishMemo"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${publishArrangeDetailDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new PublishArrangeDetail -->
    <target name="AddPublishArrangeDetail"
        description="Adds a new PublishArrangeDetail">
        <webtest name="addPublishArrangeDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editPublishArrangeDetail.html"/>
                <verifytitle description="we should see the publishArrangeDetailDetail title"
                    text=".*${publishArrangeDetailDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set publishSort" name="publishSort" value="1778275362"/>
                <setinputfield description="set specificValue" name="specificValue" value="XvFkBgQjZeVeXwCgKkAfZsGdIuMrOiEkUxLcNoYgEaHjFlUxXtPtNhXoWpXcJaLeIxXgQvLsAyAoIeMoGbZuUjQdBhHfTmTsFiWpAmTdMtIfJxOgXnThEdTqYiVoJnOg"/>
                <setinputfield description="set contractId" name="contractId" value="8538534332274268160"/>
                <setinputfield description="set customerId" name="customerId" value="7857292407794555904"/>
                <setinputfield description="set matterId" name="matterId" value="5984678376263373824"/>
                <setinputfield description="set orderDayId" name="orderDayId" value="3945257072617240576"/>
                <setinputfield description="set orderDetailId" name="orderDetailId" value="936250411185343488"/>
                <setinputfield description="set orderId" name="orderId" value="3982052554398321664"/>
                <setinputfield description="set ownerUserId" name="ownerUserId" value="7872125939505091584"/>
                <setinputfield description="set customerName" name="customerName" value="MjFoFxLzRqJxXrTvFfOuKjMjKzVpXgUkDfKkBlTtVjBoEvIrDkZoKkHzPoDnSzJeWbKsKvMoRoRuSiKkJoFtSuOyMmEcZoUtGsCeNhVcMqOySjKxKcUaLdAcSgJyLsNq"/>
                <setinputfield description="set tapeCode" name="tapeCode" value="CcLoQjMcZtLaSaQgPrBkCuFlVlLkSsQiIaQeUhZaDtLkRtGuMsCtKbIpQfNdEnIvBcZiNsHvZnPxFoRzNvBsOiKyRmBoOpXpUwPlJiKaQiPrEwGaYrPjIkUiUqCpKiAd"/>
                <setinputfield description="set matterEdit" name="matterEdit" value="BjPyCcPxVrOaGdWfZyRyYmTeKiAyFhHoErOvSpOdJjMvFkKcJuNvNxGkMrEgFoVyJvYhWnAoBnLsZhTeVkXxNmMzEoBuGyFbYfFlVqNgVhLzHyOsEpRzWwUqOiRfWeZk"/>
                <setinputfield description="set matterLength" name="matterLength" value="YuVsDtJuDoFiLtEzKcNnQkVqOnWoVqRgZuOyBdArKvOlFrOgDtVvWzAbFhMvOqAlCwJjVcPkEoUvGjRzIeEcMgBgEiChHeMwIaZcPwQiYtFhOhGbDlPbYfDjLzPlYoWn"/>
                <setinputfield description="set matterName" name="matterName" value="CcGrDnEwRnHbGbKkEeNtNvRiKrOaViMuYqLcGpYhKcUsNdWwVxTlJxEbXjGmZvBuUoVrClChBiEcCrIlZpOzJmMxYsNhFlQhDpRyAcTePuGmMuAjBnKeSiCeIwEaJmVp"/>
                <setinputfield description="set specificName" name="specificName" value="MnHoKmEbKxHxYwMyBeNuIiYeQcQjJiGrJxJyWtCoUqZeNgXiZsBiRxBkMgRhIoGwGcTvVtCiAzDnEcCqOlEjAwBwZuJvAmYlXkEcDwNjIhEwKqMhFtUdQaIuNqSuIaLp"/>
                <setinputfield description="set adverTimes" name="adverTimes" value="1913549075"/>
                <setinputfield description="set ownerUserName" name="ownerUserName" value="LnZcInDfDcLzLqQdYbGhYtOtYwWkPeUlWbLoGfFhDuGyFpVvUnRgWrWwHgZnQtClKeTmMjAxLiGzOcVrYjZkVzDwXjUxUyBtVfLpGdYhCvGuGfXxTzYjJmFsNgOsZvOv"/>
                <setinputfield description="set publishMemo" name="publishMemo" value="QsMjKaMdSfTzNdFvJmHfXtFvAcGrHiCkLnNfRfYzRmIaYkMfBaOiDvVuTyWpGwIpHuRzGeEbEgRoXcJvTuPkTmNmQsWjLlRhLmIpRkOqUhWxHqQsVsXwJqMzEuHxFsKn"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="PublishArrangeDetail List appears if save successful"
                    text=".*${publishArrangeDetailList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${publishArrangeDetail.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing publishArrangeDetail -->
    <target name="DeletePublishArrangeDetail"
        description="Deletes existing PublishArrangeDetail">
        <webtest name="deletePublishArrangeDetail">
            &config;
            <steps>
                &login;
                <invoke description="click Edit PublishArrangeDetail link" url="/editPublishArrangeDetail.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display PublishArrangeDetail List" text=".*${publishArrangeDetailList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${publishArrangeDetail.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchPublishArrangeDetails"
        description="Tests search for and displaying all publishArrangeDetails">
        <webtest name="searchPublishArrangeDetails">
            &config;
            <steps>
                &login;
                <invoke description="click View PublishArrangeDetails link" url="/publishArrangeDetails.html"/>
                <verifytitle description="we should see the publishArrangeDetailList title"
                    text=".*${publishArrangeDetailList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--PublishArrangeDetail-END-->

<!--ForetArrearage-START-->
    <!-- runs foretArrearage-related tests -->
    <target name="ForetArrearageTests"
        depends="SearchForetArrearages,EditForetArrearage,SaveForetArrearage,AddForetArrearage,DeleteForetArrearage"
        description="Call and executes all foretArrearage test cases (targets)">
        <echo>Successfully ran all ForetArrearage JSP tests!</echo>
    </target>

    <!-- Verify the edit foretArrearage screen displays without errors -->
    <target name="EditForetArrearage"
        description="Tests editing an existing ForetArrearage's information">
        <webtest name="editForetArrearage">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ForetArrearage link" url="/editForetArrearage.html?id=1"/>
                <verifytitle description="we should see the foretArrearageDetail title"
                    text=".*${foretArrearageDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a foretArrearage and then save -->
    <target name="SaveForetArrearage"
        description="Tests editing and saving a foretArrearage">
        <webtest name="saveForetArrearage">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ForetArrearage link" url="/editForetArrearage.html?id=1"/>
                <verifytitle description="we should see the foretArrearageDetail title"
                    text=".*${foretArrearageDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set customerName" name="customerName" value="customerName"/>
                <setinputfield description="set incomeCode" name="incomeCode" value="incomeCode"/>
                <setinputfield description="set incomeMode" name="incomeMode" value="incomeMode"/>
                <setinputfield description="set incomePurpose" name="incomePurpose" value="incomePurpose"/>
                <setinputfield description="set userName" name="userName" value="userName"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${foretArrearageDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new ForetArrearage -->
    <target name="AddForetArrearage"
        description="Adds a new ForetArrearage">
        <webtest name="addForetArrearage">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editForetArrearage.html"/>
                <verifytitle description="we should see the foretArrearageDetail title"
                    text=".*${foretArrearageDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set customerName" name="customerName" value="ZaRfVhDvMvAbLgHaCwZlYtEcEjInCaMc"/>
                <setinputfield description="set incomeCode" name="incomeCode" value="ZmKxYoHsHwSnVhLzOlQgQdBnBsToIiXg"/>
                <setinputfield description="set incomeDate" name="incomeDate" value="249853876"/>
                <setinputfield description="set incomeMode" name="incomeMode" value="XdYzPsEgHhGdPsPqLaFnIcYoVxNpXoEs"/>
                <setinputfield description="set incomeMoney" name="incomeMoney" value="9.668816223314081E307"/>
                <setinputfield description="set incomePurpose" name="incomePurpose" value="SjKkLfGdJpArEbFfVcEsSmUlQdTrVlUz"/>
                <setinputfield description="set payDate" name="payDate" value="188237535"/>
                <setinputfield description="set payMoney" name="payMoney" value="1.6912227777960717E308"/>
                <setinputfield description="set userName" name="userName" value="ZlZoSmGxRwOhEcMtCyByBfXpUzSiAcCq"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="ForetArrearage List appears if save successful"
                    text=".*${foretArrearageList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${foretArrearage.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing foretArrearage -->
    <target name="DeleteForetArrearage"
        description="Deletes existing ForetArrearage">
        <webtest name="deleteForetArrearage">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ForetArrearage link" url="/editForetArrearage.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display ForetArrearage List" text=".*${foretArrearageList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${foretArrearage.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchForetArrearages"
        description="Tests search for and displaying all foretArrearages">
        <webtest name="searchForetArrearages">
            &config;
            <steps>
                &login;
                <invoke description="click View ForetArrearages link" url="/foretArrearages.html"/>
                <verifytitle description="we should see the foretArrearageList title"
                    text=".*${foretArrearageList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--ForetArrearage-END-->

<!--ResourceLimit-START-->
    <!-- runs resourceLimit-related tests -->
    <target name="ResourceLimitTests"
        depends="SearchResourceLimits,EditResourceLimit,SaveResourceLimit,AddResourceLimit,DeleteResourceLimit"
        description="Call and executes all resourceLimit test cases (targets)">
        <echo>Successfully ran all ResourceLimit JSP tests!</echo>
    </target>

    <!-- Verify the edit resourceLimit screen displays without errors -->
    <target name="EditResourceLimit"
        description="Tests editing an existing ResourceLimit's information">
        <webtest name="editResourceLimit">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ResourceLimit link" url="/editResourceLimit.html?id=1"/>
                <verifytitle description="we should see the resourceLimitDetail title"
                    text=".*${resourceLimitDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a resourceLimit and then save -->
    <target name="SaveResourceLimit"
        description="Tests editing and saving a resourceLimit">
        <webtest name="saveResourceLimit">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ResourceLimit link" url="/editResourceLimit.html?id=1"/>
                <verifytitle description="we should see the resourceLimitDetail title"
                    text=".*${resourceLimitDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${resourceLimitDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new ResourceLimit -->
    <target name="AddResourceLimit"
        description="Adds a new ResourceLimit">
        <webtest name="addResourceLimit">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editResourceLimit.html"/>
                <verifytitle description="we should see the resourceLimitDetail title"
                    text=".*${resourceLimitDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set startTime" name="startTime" value="726869870"/>
                <setinputfield description="set endTime" name="endTime" value="1754826836"/>
                <setinputfield description="set limitTime" name="limitTime" value="1848945608"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="ResourceLimit List appears if save successful"
                    text=".*${resourceLimitList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${resourceLimit.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing resourceLimit -->
    <target name="DeleteResourceLimit"
        description="Deletes existing ResourceLimit">
        <webtest name="deleteResourceLimit">
            &config;
            <steps>
                &login;
                <invoke description="click Edit ResourceLimit link" url="/editResourceLimit.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display ResourceLimit List" text=".*${resourceLimitList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${resourceLimit.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchResourceLimits"
        description="Tests search for and displaying all resourceLimits">
        <webtest name="searchResourceLimits">
            &config;
            <steps>
                &login;
                <invoke description="click View ResourceLimits link" url="/resourceLimits.html"/>
                <verifytitle description="we should see the resourceLimitList title"
                    text=".*${resourceLimitList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--ResourceLimit-END-->
</project>













































































































